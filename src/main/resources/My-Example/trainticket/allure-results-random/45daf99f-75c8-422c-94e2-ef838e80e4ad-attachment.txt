🔍 ROOT CAUSE ANALYSIS
─────────────────────
**TECHNICAL ANALYSIS:**

**ROOT CAUSE:** `ts-gateway-service` initiated a `POST` request to `ts-admin-order-service` resulting in a `400 Bad Request` due to an invalid `documentType` field (specifically, a malformed string "123456789012345678901234567890") within the `Order` entity.

**FIX:**  The `ts-gateway-service` must be corrected to validate the `documentType` field *before* sending the `POST` request to `ts-admin-order-service`. Implement robust input validation, including schema enforcement and data type conversion, to prevent the injection of invalid data.  Specifically, ensure the `documentType` field is a valid integer string.