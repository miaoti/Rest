🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` due to `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'Express-RT-12345' for key 'route_type_idx'`. The exact failure location is in `com.mysql.cj.jdbc.exceptions.SQLError.createSQLException(SQLError.java:117)`.

**FIX:**
Implement duplicate detection and handling in `ts-price-service` before executing the INSERT operation. Add a pre-check query to verify if the record already exists with the same `route_type_idx` value, or implement upsert logic using `ON DUPLICATE KEY UPDATE` to prevent the constraint violation.