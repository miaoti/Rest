🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:** `ts-gateway-service` receiving invalid POST request payload resulting in 400 Bad Request error during order creation process.

**FIX:** Validate and sanitize incoming POST request body in `ts-gateway-service` before forwarding to `ts-admin-order-service`. Implement proper request schema validation and error handling for malformed payloads.