🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` and `java.sql.SQLIntegrityConstraintViolationException` due to duplicate entry 'Express-RT-12345' for key 'route_type_idx'.

**FIX:**
Implement duplicate detection and handling in the `ts-price-service` before executing the INSERT operation. Add a pre-check query to verify if the route_type 'Express-RT-12345' already exists in the `ts.price_config` table, or modify the INSERT to use `ON DUPLICATE KEY UPDATE` to gracefully handle existing records.