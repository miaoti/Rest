🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-RT-12345' for unique key 'route_type_idx'.

**FIX:**
Implement idempotent update logic in `ts-price-service` to check for existing record before insert/update, or add proper error handling to catch and gracefully handle duplicate key violations before propagating to downstream services.