🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-RT-12345' for unique key 'route_type_idx'.

**FIX:**
Implement duplicate detection logic in `ts-price-service` before executing UPDATE operations to check if record already exists with same route_type_idx, or use INSERT ... ON DUPLICATE KEY UPDATE SQL syntax to handle conflicts gracefully.