🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurred in `ts-route-service` at operation `GET /api/v1/routeservice/routes/{routeId}/` with HTTP 403 status due to `RequestRejectedException` caused by malicious string ";". The exception was thrown by Spring Security's firewall rejecting the request containing potentially malicious input.

**FIX:**
Implement proper input validation and sanitization in `ts-route-service` to handle special characters in routeId parameter. Add URL encoding/decoding for routeId values or configure Spring Security's firewall to allow legitimate semicolon characters in URLs, while maintaining security against actual malicious payloads.