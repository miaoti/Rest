🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-route-plan-service` at `RoutePlanController.getMinStopStations` which calls `RoutePlanServiceImpl.searchMinStopStations` (line 178) where a `NullPointerException` is thrown. The HTTP status 0 indicates the request failed before reaching the network layer, suggesting an unhandled exception in the service logic.

**FIX:**
Add null checks for all objects accessed in `RoutePlanServiceImpl.searchMinStopStations` around line 178, particularly for:
- Input parameters
- Database query results
- Service dependencies
- Route data structures
- Any object references that could be null

Implement defensive programming practices and add proper error handling with meaningful error responses instead of letting the NPE propagate.