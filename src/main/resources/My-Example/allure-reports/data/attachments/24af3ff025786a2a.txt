🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**  
`ts-consign-service` **GET /api/v1/consignservice/consigns/order/{id}** failed with **HTTP 403** due to **Spring Security's `RequestRejectedException`** — the URL contained a malicious string `%25` (URL-encoded `%`), which triggered the firewall's strict URL validation. The error occurred at `ThreadPoolExecutor.runWorker`, indicating the request was rejected before reaching any application logic.

**FIX:**  
1. **Sanitize URL encoding upstream** — ensure `%25` is not passed in the request path (e.g., decode double-encoded values).  
2. **Adjust Spring Security firewall** — in the gateway or consign service, configure `StrictHttpFirewall` to allow `%25` if legitimate:  
   ```java
   @Bean
   public HttpFirewall allowUrlEncodedPercent() {
       Strict