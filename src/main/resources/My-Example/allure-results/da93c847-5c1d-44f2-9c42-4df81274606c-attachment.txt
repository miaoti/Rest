🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-RT-12345' for key 'route_type_idx' constraint violation.

**FIX:**
Implement duplicate detection logic in `ts-price-service` before executing the UPDATE operation to check for existing route_type_idx values and handle conflicts appropriately, or modify the database constraint to allow the intended operation.