🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-route-plan-service` at `RoutePlanController.getMinStopStations` which calls `RoutePlanServiceImpl.searchMinStopStations` (line 178). A `NullPointerException` is thrown, indicating a null reference being accessed during route planning logic execution.

**FIX:**
Add null validation checks in `RoutePlanServiceImpl.searchMinStopStations` method before accessing any objects or collections. Specifically, verify that input parameters, station data structures, and intermediate calculation results are not null before dereferencing them.