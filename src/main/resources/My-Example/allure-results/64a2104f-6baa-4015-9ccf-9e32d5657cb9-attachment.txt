🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:** `ts-gateway-service` receiving POST request with malformed payload or invalid request parameters, resulting in 400 Bad Request response.

**FIX:** Validate incoming POST request payload structure and required fields in `ts-gateway-service` before forwarding to downstream services. Implement proper request validation middleware to reject malformed requests early in the pipeline.