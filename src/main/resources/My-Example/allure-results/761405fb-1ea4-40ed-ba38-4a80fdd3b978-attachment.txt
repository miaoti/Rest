🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-route-plan-service` at `RoutePlanController.getMinStopStations` which calls `RoutePlanServiceImpl.searchMinStopStations` (line 178) where a `NullPointerException` is thrown. The HTTP status 0 indicates the service failed to return a proper HTTP response before the timeout.

**FIX:**
Add null checks for all objects accessed in `RoutePlanServiceImpl.searchMinStopStations` method around line 178, particularly for:
1. Input parameters
2. Service dependencies 
3. Data structures returned from database/external calls
4. Any object references that may be null due to missing data or failed external service calls

The service must handle null values gracefully with proper error handling or default values instead of allowing the NPE to propagate.