🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The primary failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with `SQLIntegrityConstraintViolationException` due to duplicate entry 'Express-RT-12345' for key 'route_type_idx'. The `ts-gateway-service` propagates this failure with HTTP 500 status.

**FIX:**
Implement duplicate detection and handling in `ts-price-service` before executing the UPDATE operation. Add pre-validation check for existing route_type entries or implement UPSERT logic with appropriate error handling to prevent constraint violations.