🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:** `ts-gateway-service` receiving invalid POST request payload resulting in 400 Bad Request error during request processing.

**FIX:** Validate and sanitize incoming POST request payload format in `ts-gateway-service` before forwarding to `ts-travel-service`. Implement proper request schema validation and error handling for malformed requests.