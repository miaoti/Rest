🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The primary failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with `java.sql.SQLIntegrityConstraintViolationException` due to duplicate entry 'Express-RT-12345' for key 'route_type_idx'.

**FIX:**
Implement duplicate detection and handling in `ts-price-service` before executing the UPDATE operation. Add pre-check logic to verify if the route type 'Express-RT-12345' already exists in the `ts.price_config` table, and either skip the operation or perform an appropriate upsert strategy to prevent constraint violation.