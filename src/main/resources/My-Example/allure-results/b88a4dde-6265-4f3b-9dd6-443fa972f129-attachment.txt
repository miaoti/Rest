🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` due to `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'Regional-RT-12345' for key 'route_type_idx'`.

**FIX:**
Implement idempotent insert logic in `ts-price-service` to check for existing records before insertion, or add proper error handling to catch and gracefully manage duplicate entry exceptions. The database constraint `route_type_idx` is being violated by attempting to insert a duplicate route type identifier.