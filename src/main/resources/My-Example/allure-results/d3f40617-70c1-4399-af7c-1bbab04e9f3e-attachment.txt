🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-route-plan-service` at `RoutePlanController.getMinStopStations` which triggers `RoutePlanServiceImpl.searchMinStopStations` (line 178) where a `NullPointerException` is thrown. The HTTP status 0 indicates the request failed before reaching the network layer, suggesting the exception occurred during request processing within the service.

**FIX:**
Add null checks for all objects accessed in `RoutePlanServiceImpl.searchMinStopStations` around line 178, particularly for:
1. Input parameters (route data, station objects)
2. Any service dependencies called within the method
3. Database query results or external API responses
4. Ensure proper initialization of all objects before use

The NullPointerException indicates a missing null validation for a required field or dependency that should be validated before processing.