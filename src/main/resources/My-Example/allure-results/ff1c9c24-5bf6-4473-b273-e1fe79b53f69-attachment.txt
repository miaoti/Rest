🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` and `java.sql.SQLIntegrityConstraintViolationException` due to duplicate entry 'Express-RT-12345' for key 'route_type_idx'.

**FIX:**
Implement duplicate detection and handling in `ts-price-service` before INSERT operations. Add pre-check logic to verify if record with route_type 'Express-RT-12345' already exists, or modify the database constraint to allow updates instead of inserts, or implement UPSERT logic to handle existing records gracefully.