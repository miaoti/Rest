🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with HTTP status 0. The exact error is `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'HighSpeed-RT-12345' for key 'route_type_idx'`, indicating a primary key or unique constraint violation when attempting to insert/update a record with duplicate route type identifier.

**FIX:**
Implement idempotent operations or pre-checks in `ts-price-service` before executing `UPDATE ts.price_config` to verify if the route type 'HighSpeed-RT-12345' already exists. Either:
1. Add a SELECT query to check existence before UPDATE/INSERT
2. Use INSERT ... ON DUPLICATE KEY UPDATE SQL syntax
3. Implement proper error handling to catch and gracefully manage constraint violations in the calling service `ts-admin-basic-info-service`