🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-92708982-77af-4318-be25-57ccb0ff69ae' for unique key 'route_type_idx'.

**FIX:**
Implement idempotent update logic in `ts-price-service` to check for existing record before insert/update, or add proper duplicate detection and handling before executing the database operation.