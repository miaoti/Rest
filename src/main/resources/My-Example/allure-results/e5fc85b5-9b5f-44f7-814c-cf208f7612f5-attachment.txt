🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-admin-order-service` at `AdminOrderController.addOrder` when attempting to deserialize JSON payload. The specific error is a `JsonMappingException` where the string value "abc" cannot be converted to an integer type for the `coachNumber` field in the `Order` entity.

**FIX:**
Validate and sanitize input data in `ts-gateway-service` before forwarding to `ts-admin-order-service`. The `coachNumber` field must be validated to ensure it contains only numeric values or implement proper input validation in `ts-admin-order-service` to handle non-numeric strings gracefully.