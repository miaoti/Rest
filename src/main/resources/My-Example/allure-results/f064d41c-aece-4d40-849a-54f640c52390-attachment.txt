🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:** `ts-gateway-service` failing POST operation with HTTP 400 status code during request processing.

**FIX:** Validate request payload format and required fields in `ts-gateway-service` POST endpoint to ensure compliance with expected API schema before forwarding to `ts-travel-service`.