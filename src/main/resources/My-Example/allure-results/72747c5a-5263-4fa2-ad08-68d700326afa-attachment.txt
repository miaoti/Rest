🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-route-plan-service` at `RoutePlanController.getMinStopStations` which calls `RoutePlanServiceImpl.searchMinStopStations` (line 178) where a `NullPointerException` is thrown. The HTTP status 0 indicates the request failed before reaching the network layer, suggesting the exception occurred during request processing within the service.

**FIX:**
Add null checks for all objects accessed in `RoutePlanServiceImpl.searchMinStopStations` around line 178, particularly for:
1. Input parameters
2. Service dependencies (ts-route-service, ts-travel-plan-service)
3. Any objects returned from external API calls
4. Route data structures being processed

The NullPointerException indicates an unhandled null reference in the route planning logic.