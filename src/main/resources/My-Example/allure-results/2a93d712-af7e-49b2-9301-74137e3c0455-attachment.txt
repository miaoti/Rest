🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` due to `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'Express-RT-12345' for key 'route_type_idx'`.

**FIX:**
Implement duplicate detection and handling in `ts-price-service` before INSERT operations. Add pre-check logic to verify if entry exists with same `route_type_idx` value, or configure INSERT to use `ON DUPLICATE KEY UPDATE` strategy to prevent constraint violation.