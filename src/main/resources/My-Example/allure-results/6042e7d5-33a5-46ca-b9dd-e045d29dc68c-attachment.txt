🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` due to `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'HighSpeed-RT-12345' for key 'route_type_idx'`.

**FIX:**
Implement duplicate detection and handling in `ts-price-service` before executing the INSERT operation. Add a pre-check query to verify if the entry already exists, or modify the INSERT to use `INSERT IGNORE` or `ON DUPLICATE KEY UPDATE` to prevent the constraint violation.