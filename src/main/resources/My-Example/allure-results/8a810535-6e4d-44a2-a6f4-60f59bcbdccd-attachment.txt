🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-route-plan-service` at `RoutePlanController.getMinStopStations` which calls `RoutePlanServiceImpl.searchMinStopStations` (line 178) where a `NullPointerException` is thrown. The HTTP status 0 indicates the request failed before reaching the network layer, suggesting the exception occurred during request processing within the service.

**FIX:**
Add null checks for all objects referenced in `RoutePlanServiceImpl.searchMinStopStations` at line 178, particularly for:
1. Input parameters being passed to the method
2. Any objects returned from service calls or data fetch operations
3. Collections/arrays that may be null when accessed
4. Ensure proper initialization of all variables before use

The service should implement defensive programming practices to handle null inputs and return values from upstream services.