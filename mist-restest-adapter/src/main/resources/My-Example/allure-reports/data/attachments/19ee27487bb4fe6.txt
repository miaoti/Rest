🔍 ROOT CAUSE ANALYSIS
─────────────────────
ROOT CAUSE: `ts-gateway-service` PUT operation failed with HTTP 403 (Forbidden) at 1.116ms execution time. The 403 status indicates the gateway service rejected the request due to authentication/authorization failure before reaching `ts-admin-basic-info-service`. The extremely short execution time (1.116ms) confirms the request was blocked at the gateway layer, not forwarded to the downstream service.

FIX: Verify authentication token/API key validity in the PUT request to `ts-gateway-service`. Check gateway service RBAC configuration to ensure the request principal has PUT permission on the target endpoint. Validate gateway service security rules and route filters are not incorrectly blocking the request.