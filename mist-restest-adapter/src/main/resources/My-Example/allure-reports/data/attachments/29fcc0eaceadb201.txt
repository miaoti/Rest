🔍 ROOT CAUSE ANALYSIS
─────────────────────
ROOT CAUSE: `ts-gateway-service` PUT operation failed with HTTP 403 Forbidden in 1.114ms. The gateway received an authorization rejection before forwarding to `ts-admin-basic-info-service`. This is likely due to missing or invalid authentication/authorization token in the PUT request, or the gateway's security filter rejecting the request.

FIX: Verify that the PUT request includes a valid JWT or API key in the Authorization header. Check `ts-gateway-service` security configuration (e.g., Spring Security filter chain) to ensure the endpoint is permitted for the caller's role. If using OAuth2 or custom auth, confirm token validation logic and expiration.