🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with `HTTP Status: 0` due to `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'Express-RT-12345' for key 'route_type_idx'`.

**FIX:**
Implement idempotent insert logic in `ts-price-service` to check for existing records before insertion, or use `INSERT IGNORE`/`ON DUPLICATE KEY UPDATE` MySQL syntax to handle duplicate key scenarios gracefully.