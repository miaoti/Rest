🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `INSERT ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-RT-12345' for unique key 'route_type_idx'.

**FIX:**
Implement duplicate detection and handling in the `ts-price-service` before INSERT operations. Add pre-validation logic to check for existing route_type entries before attempting insertion, or modify the INSERT to use ON DUPLICATE KEY UPDATE behavior.