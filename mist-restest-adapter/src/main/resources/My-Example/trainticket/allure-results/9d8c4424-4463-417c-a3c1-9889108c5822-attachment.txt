🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-RT-12345' for key 'route_type_idx' in MySQL database.

**FIX:**
Implement duplicate detection and handling in the `ts-price-service` before executing the UPDATE operation. Add pre-validation logic to check for existing route_type entries and either:
1. Update existing record if it's a legitimate update
2. Return appropriate HTTP 409 Conflict status to client
3. Implement upsert logic using INSERT ... ON DUPLICATE KEY UPDATE syntax