🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurs in `ts-price-service` during `UPDATE ts.price_config` operation with SQLIntegrityConstraintViolationException due to duplicate entry 'Express-RT-12345' for unique key 'route_type_idx'.

**FIX:**
Implement duplicate detection logic in `ts-price-service` before executing the UPDATE operation to check for existing records with the same route_type value, or modify the application logic to handle upsert operations using INSERT ... ON DUPLICATE KEY UPDATE syntax.