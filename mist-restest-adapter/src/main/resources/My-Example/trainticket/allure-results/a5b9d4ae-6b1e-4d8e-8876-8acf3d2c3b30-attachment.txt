🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:**
The failure occurred in `ts-price-service` during `UPDATE ts.price_config` operation with HTTP status 0. The exact error is `java.sql.SQLIntegrityConstraintViolationException: Duplicate entry 'Regional-../../etc/passwd' for key 'route_type_idx'`, indicating a database constraint violation where a duplicate value was attempted to be inserted into the `route_type_idx` unique key column.

**FIX:**
Implement input validation and sanitization in `ts-price-service` to prevent malicious or duplicate values from being passed to the database. Add pre-check logic to verify existence of `route_type` values before insertion, and implement proper error handling to reject invalid inputs before database operations.