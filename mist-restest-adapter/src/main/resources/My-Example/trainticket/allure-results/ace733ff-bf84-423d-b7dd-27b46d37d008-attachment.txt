🔍 ROOT CAUSE ANALYSIS
─────────────────────
**ROOT CAUSE:** `ts-gateway-service` failing DELETE operation with HTTP 400 status during `DELETE /api/travel/123` endpoint call.

**FIX:** Validate request payload format and required parameters in `ts-gateway-service` DELETE handler before forwarding to `ts-admin-travel-service`.