package trainticket_twostage_test.TrainTicketTwoStageTest_1767469626811;

import io.restassured.RestAssured;
import io.restassured.response.Response;
import io.restassured.specification.RequestSpecification;
import org.junit.BeforeClass;
import org.junit.Test;
import org.junit.AssumptionViolatedException;
import java.util.concurrent.atomic.AtomicBoolean;
import java.util.concurrent.atomic.AtomicInteger;
import java.util.Map;
import java.util.*;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import org.hamcrest.Matchers;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import org.json.JSONObject;
import org.json.JSONArray;
import es.us.isa.restest.analysis.TraceErrorAnalyzer;
import es.us.isa.restest.inputs.smart.ParameterErrorAnalyzer;
import es.us.isa.restest.inputs.smart.InputFetchRegistry;
import es.us.isa.restest.inputs.smart.ParameterError;
import static org.junit.Assert.*;
import es.us.isa.restest.testcases.MultiServiceTestCase;
import io.qameta.allure.Allure;
import io.qameta.allure.model.Status;

public class post_api_v1_adminorderservice_adminorder_101 {

    // Jaeger configuration
    private static final boolean JAEGER_ENABLED = Boolean.parseBoolean(System.getProperty("jaeger.enabled", "true"));
    // ⚠️ CRITICAL: Use /traces/{id} endpoint to fetch COMPLETE trace with all spans
    // /traces endpoint returns list with summaries (limited spans)
    // /traces/{id} endpoint returns COMPLETE trace tree with all spans
    private static final String JAEGER_BASE_URL = System.getProperty("jaeger.base.url", "http://129.62.148.112:30005/jaeger/ui/api");
    private static final String JAEGER_LOOKBACK = System.getProperty("jaeger.lookback", "10m");

    private static void attachJaegerTrace(String service, String method, String path, long requestStartMicros, Map<String, String> stepParameters, boolean isStepFailed) {
        if (!JAEGER_ENABLED) return;
        try {
            String operation = method + " " + path;
            String opEncoded = URLEncoder.encode(operation, StandardCharsets.UTF_8);
            String svcEncoded = URLEncoder.encode(service, StandardCharsets.UTF_8);
            // Create very precise time window for unique trace identification (10 seconds before, 30 seconds after)
            long start = requestStartMicros - (10L * 1000L * 1000L); // 10 seconds earlier (us)
            long end = requestStartMicros + (30L * 1000L * 1000L); // 30 seconds later (us)
            if (start < 0) start = 0;
            
            // Try multiple query strategies to find traces
            // 🔥 FIX: Query gateway services FIRST to get complete distributed traces
            // Gateway traces include all downstream service spans, but backend traces may not include gateway spans
            String[] queryUrls = {
                // Strategy 1: ts-gateway-service with time window (PRIORITY - contains full distributed trace)
                JAEGER_BASE_URL + "/traces?limit=100&service=ts-gateway-service&start=" + start + "&end=" + end,
                // Strategy 2: ts-gateway-service with method (more specific)
                JAEGER_BASE_URL + "/traces?limit=50&service=ts-gateway-service&operation=" + URLEncoder.encode(method, StandardCharsets.UTF_8) + "&start=" + start + "&end=" + end,
                // Strategy 3: Other common gateway services
                JAEGER_BASE_URL + "/traces?limit=100&service=gateway-service&start=" + start + "&end=" + end,
                JAEGER_BASE_URL + "/traces?limit=100&service=api-gateway&start=" + start + "&end=" + end,
                // Strategy 4: Broad search for any traces with API calls
                JAEGER_BASE_URL + "/traces?limit=200&start=" + start + "&end=" + end,
                // Strategy 5: Target service with exact operation (fallback if gateway not found)
                JAEGER_BASE_URL + "/traces?limit=50&service=" + svcEncoded + "&operation=" + opEncoded + "&start=" + start + "&end=" + end,
                // Strategy 6: Target service with method only
                JAEGER_BASE_URL + "/traces?limit=50&service=" + svcEncoded + "&operation=" + URLEncoder.encode(method, StandardCharsets.UTF_8) + "&start=" + start + "&end=" + end,
                // Strategy 7: Target service in time window
                JAEGER_BASE_URL + "/traces?limit=100&service=" + svcEncoded + "&start=" + start + "&end=" + end,
                // Strategy 8: Recent traces with lookback (last resort)
                JAEGER_BASE_URL + "/traces?limit=200&lookback=" + JAEGER_LOOKBACK
            };
            
            HttpClient client = HttpClient.newHttpClient();
            StringBuilder debugInfo = new StringBuilder();
            debugInfo.append("🔍 Jaeger Query Debug Info:\n");
            debugInfo.append("Target Service: ").append(service).append("\n");
            debugInfo.append("Method: ").append(method).append("\n");
            debugInfo.append("Path: ").append(path).append("\n");
            debugInfo.append("Operation: ").append(operation).append("\n");
            debugInfo.append("Request Start (μs): ").append(requestStartMicros).append("\n");
            debugInfo.append("Search Window: ").append(start).append(" to ").append(end).append(" (μs)\n");
            debugInfo.append("Time Window: ").append((end - start) / 1000000).append(" seconds\n");
            debugInfo.append("🚪 Gateway Strategy: Searching ts-gateway-service first (HTTP traces usually there)\n");
            debugInfo.append("⏱️ Test Separation: 2-second delay enforced between executions for unique traces\n");
            debugInfo.append("🔄 Trace Propagation: 3-second delay after execution for Jaeger indexing\n");
            debugInfo.append("🔄 Retry Strategy: Multiple attempts to find current execution trace\n\n");
            
            // 🔄 Retry mechanism to find current execution trace
            boolean foundCurrentTrace = false;
            int maxRetries = 3;
            JSONObject globalBestTrace = null;  // Track best trace across all queries
            long globalBestDiff = Long.MAX_VALUE;
            int globalBestScore = -1;
            
            for (int retry = 0; retry < maxRetries && !foundCurrentTrace; retry++) {
                if (retry > 0) {
                    debugInfo.append("\n🔄 RETRY ").append(retry).append(": Searching again for current execution trace...\n");
                    try { Thread.sleep(2000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                }
                
            for (int queryIdx = 0; queryIdx < queryUrls.length; queryIdx++) {
                String url = queryUrls[queryIdx];
                debugInfo.append("Query ").append(queryIdx + 1).append(": ").append(url).append("\n");
                
                HttpRequest req = HttpRequest.newBuilder(java.net.URI.create(url)).GET().build();
                HttpResponse<String> res = client.send(req, HttpResponse.BodyHandlers.ofString());
                debugInfo.append("Response Status: ").append(res.statusCode()).append("\n");
                
                if (res.statusCode() >= 200 && res.statusCode() < 300) {
                    String body = res.body();
                    debugInfo.append("Response Body Length: ").append(body.length()).append("\n");
                    
                    try {
                        JSONObject json = new JSONObject(body);
                        if (json.has("data")) {
                            JSONArray data = json.getJSONArray("data");
                            debugInfo.append("Traces Found: ").append(data.length()).append("\n");
                            
                            if (data.length() > 0) {
                                // Smart trace selection - prioritize traces with actual API calls
                                JSONObject bestTrace = null;
                                long bestDiff = Long.MAX_VALUE;
                                int bestTraceIdx = -1;
                                int bestScore = -1;
                                
                                debugInfo.append("Analyzing ").append(data.length()).append(" traces for API calls...\n");
                                
                                for (int i = 0; i < data.length(); i++) {
                                    JSONObject trace = data.getJSONObject(i);
                                    JSONArray spans = trace.optJSONArray("spans");
                                    if (spans == null) continue;
                                    
                                    int traceScore = 0;
                                    int apiCallCount = 0;
                                    boolean hasTargetOperation = false;
                                    long closestTime = Long.MAX_VALUE;
                                    
                                    // Analyze all spans in this trace
                                    for (int s = 0; s < spans.length(); s++) {
                                        JSONObject span = spans.getJSONObject(s);
                                        String opName = span.optString("operationName", "");
                                        long spanStart = span.optLong("startTime", 0L);
                                        
                                        // Check if this span is an actual API call
                                        boolean isApiSpan = false;
                                        String spanMethod = "";
                                        String spanPath = "";
                                        
                                        JSONArray tags = span.optJSONArray("tags");
                                        if (tags != null) {
                                            for (int t = 0; t < tags.length(); t++) {
                                                JSONObject tag = tags.getJSONObject(t);
                                                String key = tag.optString("key");
                                                String value = tag.optString("value");
                                                if ("http.method".equals(key)) spanMethod = value;
                                                if ("http.target".equals(key) || "http.url".equals(key)) spanPath = value;
                                            }
                                        }
                                        
                                        // Check if this is an API call
                                        if ((!spanMethod.isEmpty() && !spanPath.isEmpty() && spanPath.contains("/api/")) ||
                                            opName.contains("/api/") ||
                                            opName.matches("(GET|POST|PUT|DELETE|PATCH)\\s+/api/.*")) {
                                            isApiSpan = true;
                                            apiCallCount++;
                                        }
                                        
                                        // Check if this matches our target operation
                                        boolean isTargetOp = opName.equals(operation) ||
                                            (spanMethod.equalsIgnoreCase(method) && spanPath.contains(path)) ||
                                            (opName.contains(method) && opName.contains(path));
                                        
                                        if (isTargetOp) {
                                            hasTargetOperation = true;
                                            if (spanStart > 0L) {
                                                long timeDiff = Math.abs(spanStart - requestStartMicros);
                                                closestTime = Math.min(closestTime, timeDiff);
                                            }
                                        }
                                    }
                                    
                                    // Enhanced scoring - HEAVILY prioritize recent execution timing
                                    if (hasTargetOperation) traceScore += 1000; // Target operation match
                                    traceScore += apiCallCount * 5; // More API calls = better (reduced weight)
                                    
                                    // CRITICAL: Ultra-precise time proximity scoring (test separation enforced)
                                    if (closestTime < Long.MAX_VALUE) {
                                        if (closestTime < 1000000L) traceScore += 5000; // Within 1 second (CURRENT execution)
                                        else if (closestTime < 2000000L) traceScore += 3000; // Within 2 seconds (EXACT execution)
                                        else if (closestTime < 5000000L) traceScore += 1500; // Within 5 seconds (very recent)
                                        else if (closestTime < 10000000L) traceScore += 500; // Within 10 seconds
                                        else if (closestTime < 20000000L) traceScore += 200; // Within 20 seconds
                                        else if (closestTime < 30000000L) traceScore += 100; // Within 30 seconds
                                    }
                                    
                                    debugInfo.append("Trace ").append(i).append(": score=").append(traceScore);
                                    debugInfo.append(", APIs=").append(apiCallCount);
                                    debugInfo.append(", hasTarget=").append(hasTargetOperation);
                                    debugInfo.append(", timeDiff=").append(closestTime == Long.MAX_VALUE ? "N/A" : closestTime + "μs");
                                    debugInfo.append(", spans=").append(spans.length()).append("\n");
                                    
                                    // Show first few operations for debugging
                                    debugInfo.append("  Operations: ");
                                    for (int opIdx = 0; opIdx < Math.min(spans.length(), 5); opIdx++) {
                                        JSONObject span = spans.getJSONObject(opIdx);
                                        String opName = span.optString("operationName", "");
                                        if (opIdx > 0) debugInfo.append(", ");
                                        debugInfo.append(opName.length() > 30 ? opName.substring(0, 27) + "..." : opName);
                                    }
                                    if (spans.length() > 5) debugInfo.append(", +").append(spans.length() - 5).append(" more");
                                    debugInfo.append("\n");
                                    
                                    // Select best trace: TIMING is the primary discriminator for recent requests
                                    boolean shouldSelectTrace = false;
                                    
                                    if (traceScore > 0) { // Must have API calls
                                        if (bestTrace == null) {
                                            shouldSelectTrace = true;
                                        } else {
                                            // For recent traces (within 10 seconds), prioritize timing over score
                                            boolean currentIsRecent = closestTime < 10000000L; // 10 seconds (post-delay execution)
                                            boolean bestIsRecent = bestDiff < 10000000L;
                                            
                                            if (currentIsRecent && !bestIsRecent) {
                                                shouldSelectTrace = true; // Always prefer recent traces
                                            } else if (!currentIsRecent && bestIsRecent) {
                                                shouldSelectTrace = false; // Keep the recent one
                                            } else if (currentIsRecent && bestIsRecent) {
                                                // Both recent: pick the closest in time
                                                shouldSelectTrace = closestTime < bestDiff;
                                            } else {
                                                // Both old: use score then timing
                                                shouldSelectTrace = traceScore > bestScore || 
                                                    (traceScore == bestScore && closestTime < bestDiff);
                                            }
                                        }
                                    }
                                    
                                    if (shouldSelectTrace) {
                                        bestScore = traceScore;
                                        bestDiff = closestTime;
                                        bestTrace = trace;
                                        bestTraceIdx = i;
                                    }
                                }
                                
                                debugInfo.append("\nSelected trace ").append(bestTraceIdx).append(" with score ").append(bestScore);
                                if (bestScore > 0) {
                                    // Check if this is a current execution trace (within 3 seconds)
                                    boolean isCurrentTrace = bestDiff < 3000000L; // 3 seconds
                                    if (isCurrentTrace) {
                                        debugInfo.append(" (CURRENT EXECUTION - perfect match!)\n");
                                        foundCurrentTrace = true;
                                    } else {
                                        debugInfo.append(" (matched but not current - possibly previous execution)\n");
                                    }
                                    
                                    // Update global best trace if this is better
                                    boolean shouldUpdateGlobal = false;
                                    if (globalBestTrace == null) {
                                        shouldUpdateGlobal = true;
                                    } else if (isCurrentTrace && globalBestDiff >= 3000000L) {
                                        shouldUpdateGlobal = true; // Always prefer current over non-current
                                    } else if (isCurrentTrace && globalBestDiff < 3000000L) {
                                        shouldUpdateGlobal = bestDiff < globalBestDiff; // Both current: pick closer
                                    } else if (!isCurrentTrace && globalBestDiff >= 3000000L) {
                                        // Both non-current: pick better score or closer time
                                        shouldUpdateGlobal = bestScore > globalBestScore || 
                                            (bestScore == globalBestScore && bestDiff < globalBestDiff);
                                    }
                                    
                                    if (shouldUpdateGlobal) {
                                        // Fetch COMPLETE trace to replace summary
                                        if (bestTrace != null) {
                                            String traceId = bestTrace.optString("traceID", "");
                                            if (!traceId.isEmpty()) {
                                                debugInfo.append("\n🔄 Fetching complete trace for ID: ").append(traceId).append("\n");
                                                String traceDetailUrl = JAEGER_BASE_URL + "/traces/" + traceId;
                                                HttpRequest traceReq = HttpRequest.newBuilder(java.net.URI.create(traceDetailUrl)).GET().build();
                                                HttpResponse<String> traceRes = client.send(traceReq, HttpResponse.BodyHandlers.ofString());
                                                
                                                if (traceRes.statusCode() >= 200 && traceRes.statusCode() < 300) {
                                                    JSONObject completeTraceResponse = new JSONObject(traceRes.body());
                                                    if (completeTraceResponse.has("data") && completeTraceResponse.getJSONArray("data").length() > 0) {
                                                        bestTrace = completeTraceResponse.getJSONArray("data").getJSONObject(0);
                                                        debugInfo.append("✅ Complete trace fetched with ").append(bestTrace.optJSONArray("spans").length()).append(" spans\n");
                                                    } else {
                                                        debugInfo.append("⚠️  Complete trace response has no data, using summary trace\n");
                                                    }
                                                } else {
                                                    debugInfo.append("⚠️  Failed to fetch complete trace (status: ").append(traceRes.statusCode()).append("), using summary trace\n");
                                                }
                                            }
                                        }
                                        
                                        // Update global best
                                        globalBestTrace = bestTrace;
                                        globalBestDiff = bestDiff;
                                        globalBestScore = bestScore;
                                        debugInfo.append("✅ Updated global best trace (score: ").append(bestScore).append(", timeDiff: ").append(bestDiff / 1000000.0).append("s)\n");
                                    }
                                    
                                    // If this is a current trace, we can stop retrying
                                    if (isCurrentTrace) {
                                        debugInfo.append("✅ Found current execution trace - stopping search\n");
                                        break; // Exit query loop
                                    } else {
                                        debugInfo.append("⏭️ Continuing to search for more recent trace...\n");
                                    }
                                } else {
                                    debugInfo.append(" (no API calls found)\n");
                                    debugInfo.append("\n⚠️ This query found traces but no API calls. Continuing to next strategy...\n");
                                }
                            } else {
                                debugInfo.append("No traces found in response\n");
                            }
                        } else {
                            debugInfo.append("No 'data' field in response\n");
                        }
                    } catch (Exception e) {
                        debugInfo.append("JSON Parse Error: ").append(e.getMessage()).append("\n");
                    }
                    
                    // If this query didn't work, try next one
                    debugInfo.append("Query ").append(queryIdx + 1).append(" failed, trying next...\n\n");
                } else {
                    debugInfo.append("HTTP Error: ").append(res.statusCode()).append("\n\n");
                }
            }
            
            // If we found a current trace, exit retry loop
            if (foundCurrentTrace) {
                debugInfo.append("✅ Found current execution trace, stopping retries.\n");
                break; // Exit retry loop
            }
            }
            
            // After all searches, analyze and attach the BEST trace found (if any)
            if (globalBestTrace != null) {
                debugInfo.append("\n✅ Using best trace found (score: ").append(globalBestScore)
                           .append(", timeDiff: ").append(globalBestDiff / 1000000.0).append("s)\n");
                
                String traceTable = generateTraceTable(globalBestTrace);
                String traceSummary = generateTraceSummary(globalBestTrace);
                TraceErrorAnalyzer.ErrorAnalysisResult errorAnalysis = TraceErrorAnalyzer.analyzeTrace(globalBestTrace);
                String errorReport = TraceErrorAnalyzer.generateErrorReport(errorAnalysis);
                
                if (errorAnalysis.hasErrors()) {
                    // Trace contains technical errors (error=true tags)
                    analyzeAndRecordParameterErrors(globalBestTrace, stepParameters);
                    String intelligentAnalysis = TraceErrorAnalyzer.generateIntelligentAnalysis(errorAnalysis, globalBestTrace);
                    if (intelligentAnalysis != null && !intelligentAnalysis.trim().isEmpty()) {
                        Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Based on Trace)", "text/plain", intelligentAnalysis);
                    }
                    Allure.addAttachment("🔗 API Call Trace", "text/plain", traceTable);
                } else if (isStepFailed) {
                    // Test failed but trace has no technical errors - likely business logic validation failure
                    StringBuilder analysisMsg = new StringBuilder();
                    analysisMsg.append("═══════════════════════════════════════════════════════════════════════\n");
                    analysisMsg.append("🔍 ROOT CAUSE ANALYSIS\n");
                    analysisMsg.append("═══════════════════════════════════════════════════════════════════════\n\n");
                    analysisMsg.append("**ANALYSIS STATUS:**\n");
                    analysisMsg.append("The distributed trace for this failed test execution was successfully retrieved,\n");
                    analysisMsg.append("however, it does not contain any technical error indicators (error tags).\n\n");
                    analysisMsg.append("**LIKELY CAUSE:**\n");
                    analysisMsg.append("This failure is most likely due to business logic validation rather than\n");
                    analysisMsg.append("a technical error. The service processed the request successfully but\n");
                    analysisMsg.append("rejected it based on application-level validation rules (e.g., invalid\n");
                    analysisMsg.append("business state, constraint violations, or semantic validation failures).\n\n");
                    analysisMsg.append("**RECOMMENDATIONS:**\n");
                    analysisMsg.append("• Review the response body in the '📥 Response' section for validation error messages\n");
                    analysisMsg.append("• Check the HTTP status code (e.g., 400 Bad Request, 422 Unprocessable Entity)\n");
                    analysisMsg.append("• Examine the '🔗 API Call Trace' section to identify which service rejected the request\n");
                    analysisMsg.append("• Verify input parameters against business rules and constraints\n");
                    analysisMsg.append("• Consult the API documentation for validation requirements\n\n");
                    analysisMsg.append("For detailed trace information, see the '📈 Raw Trace Data' section below.\n");
                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Based on Trace)", "text/plain", analysisMsg.toString());
                    Allure.addAttachment("🔗 API Call Trace", "text/plain", traceTable);
                } else {
                    // Test succeeded and trace has no errors
                    Allure.addAttachment("🔗 API Call Trace", "text/plain", traceTable);
                }
                Allure.addAttachment("📊 Trace Summary", "text/plain", traceSummary);
                Allure.addAttachment("📈 Raw Trace Data", "application/json", globalBestTrace.toString());
                Allure.addAttachment("🔍 Query Debug Info", "text/plain", debugInfo.toString());
            } else {
                // No traces found with any query after all retries
                if (!foundCurrentTrace) {
                    debugInfo.append("❌ No current execution traces found after ").append(maxRetries).append(" retries\n");
                }
                debugInfo.append("❌ No traces found with any query strategy");
                Allure.addAttachment("🔍 Jaeger Query Debug (No Traces)", "text/plain", debugInfo.toString());
            }
            
        } catch (Exception e) {
            Allure.addAttachment("📈 Jaeger Trace Error", "text/plain", "Error: " + e.toString() + "\nService: " + service + "\nMethod: " + method + "\nPath: " + path);
        }
    }

    private static String generateTraceTable(JSONObject trace) {
        try {
            JSONArray spans = trace.getJSONArray("spans");
            JSONObject processes = trace.optJSONObject("processes");
            String traceId = trace.optString("traceID", "");
            
            StringBuilder table = new StringBuilder();
            table.append("----------------------------------------------------------------------------------------\n");
            table.append("                           🔗 MICROSERVICE API CALL TRACE                           \n");
            table.append("----------------------------------------------------------------------------------------\n");
            if (!traceId.isEmpty()) {
                table.append("Trace ID: ").append(traceId).append("\n");
                // Convert API endpoint to UI endpoint for browser link
                // Remove /api from end if present, keep everything else (including /jaeger/ui if present)
                String uiBase = JAEGER_BASE_URL;
                if (uiBase.endsWith("/api")) {
                    uiBase = uiBase.substring(0, uiBase.length() - 4);  // Remove "/api"
                }
                table.append("Jaeger UI: ").append(uiBase).append("/trace/").append(traceId).append("\n");
                table.append("────────────────────────────────────────────────────────────────────────────────────────\n");
            }
            
            // Build hierarchical API call structure
            Map<String, JSONObject> apiSpanById = new HashMap<>();
            Map<String, List<String>> apiChildren = new HashMap<>();
            Map<String, String> apiParentOf = new HashMap<>();
            
            // First pass: collect all API spans and build relationships
            for (int i = 0; i < spans.length(); i++) {
                JSONObject span = spans.getJSONObject(i);
                if (isApiCall(span)) {
                    String spanId = span.getString("spanID");
                    apiSpanById.put(spanId, span);
                    apiChildren.put(spanId, new ArrayList<>());
                }
            }
            
            // Second pass: establish parent-child relationships for API calls
            for (int i = 0; i < spans.length(); i++) {
                JSONObject span = spans.getJSONObject(i);
                if (isApiCall(span)) {
                    String spanId = span.getString("spanID");
                    String parentId = span.optString("parentSpanId", null);
                    
                    // Look for parent in references if not directly available
                    if (parentId == null || parentId.isEmpty()) {
                        JSONArray refs = span.optJSONArray("references");
                        if (refs != null) {
                            for (int r = 0; r < refs.length(); r++) {
                                JSONObject ref = refs.getJSONObject(r);
                                if ("CHILD_OF".equalsIgnoreCase(ref.optString("refType"))) {
                                    parentId = ref.optString("spanID");
                                    break;
                                }
                            }
                        }
                    }
                    
                    // Only connect if parent is also an API call
                    if (parentId != null && apiSpanById.containsKey(parentId)) {
                        apiChildren.get(parentId).add(spanId);
                        apiParentOf.put(spanId, parentId);
                    }
                }
            }
            
            // Find root API calls (those without API parents)
            List<String> rootApiCalls = new ArrayList<>();
            for (String apiId : apiSpanById.keySet()) {
                if (!apiParentOf.containsKey(apiId)) {
                    rootApiCalls.add(apiId);
                }
            }
            
            // Sort children by start time
            Comparator<String> byStartTime = (a, b) -> Long.compare(
                apiSpanById.get(a).optLong("startTime", 0L),
                apiSpanById.get(b).optLong("startTime", 0L)
            );
            for (List<String> children : apiChildren.values()) {
                children.sort(byStartTime);
            }
            rootApiCalls.sort(byStartTime);
            
            if (rootApiCalls.isEmpty()) {
                table.append("⚠️  No API calls found in this trace\n");
                table.append("   This might be an internal operation or framework activity\n");
            } else {
                table.append("🌐 API CALL HIERARCHY (Parent → Child Relationships)\n");
                table.append("────────────────────────────────────────────────────────────────────────────────────────\n");
                
                // Render hierarchical tree
                AtomicInteger apiCounter = new AtomicInteger(1);
                for (String rootId : rootApiCalls) {
                    renderApiHierarchy(table, rootId, apiSpanById, apiChildren, processes, 0, apiCounter, "");
                }
            }
            
            table.append("████████████████████████████████████████████████████████████████████████████████████████\n");
            return table.toString();
        } catch (Exception e) {
            return "❌ Failed to generate trace table: " + e.getMessage();
        }
    }

    private static String generateTraceSummary(JSONObject trace) {
        try {
            JSONArray spans = trace.getJSONArray("spans");
            JSONObject processes = trace.optJSONObject("processes");
            String traceId = trace.optString("traceID", "");
            
            StringBuilder summary = new StringBuilder();
            summary.append("🎯 TRACE SUMMARY\n");
            summary.append("═══════════════\n\n");
            
            // Count API calls by status
            int totalApis = 0;
            int successApis = 0;
            int errorApis = 0;
            Set<String> services = new HashSet<>();
            long totalDuration = 0L;
            long minDuration = Long.MAX_VALUE;
            long maxDuration = 0L;
            
            for (int i = 0; i < spans.length(); i++) {
                JSONObject span = spans.getJSONObject(i);
                if (isApiCall(span)) {
                    totalApis++;
                    String[] httpInfo = extractHttpInfo(span);
                    String status = httpInfo[2];
                    
                    if (status.startsWith("2")) successApis++;
                    else if (status.startsWith("4") || status.startsWith("5")) errorApis++;
                    
                    services.add(getServiceName(span, processes));
                    
                    long duration = span.optLong("duration", 0L);
                    if (duration > 0) {
                        totalDuration += duration;
                        minDuration = Math.min(minDuration, duration);
                        maxDuration = Math.max(maxDuration, duration);
                    }
                }
            }
            
            summary.append("📊 API Call Statistics:\n");
            summary.append("   Total API Calls: ").append(totalApis).append("\n");
            summary.append("   ✅ Successful: ").append(successApis).append("\n");
            summary.append("   ❌ Failed: ").append(errorApis).append("\n");
            summary.append("   🏢 Services Involved: ").append(services.size()).append("\n\n");
            
            if (totalApis > 0) {
                summary.append("⏱️ Performance Metrics:\n");
                summary.append("   Total Duration: ").append(formatDuration(totalDuration)).append("\n");
                summary.append("   Average Duration: ").append(formatDuration(totalDuration / totalApis)).append("\n");
                if (minDuration != Long.MAX_VALUE) {
                    summary.append("   Fastest Call: ").append(formatDuration(minDuration)).append("\n");
                    summary.append("   Slowest Call: ").append(formatDuration(maxDuration)).append("\n");
                }
                summary.append("\n");
            }
            
            summary.append("🏢 Services in Trace:\n");
            for (String service : services) {
                summary.append("   • ").append(service).append("\n");
            }
            
            return summary.toString();
        } catch (Exception e) {
            return "❌ Failed to generate trace summary: " + e.getMessage();
        }
    }

    private static boolean isApiCall(JSONObject span) {
        String opName = span.optString("operationName", "");
        JSONArray tags = span.optJSONArray("tags");
        
        boolean hasHttpMethod = false;
        boolean hasHttpUrl = false;
        String httpUrl = "";
        String httpMethod = "";
        
        if (tags != null) {
            for (int t = 0; t < tags.length(); t++) {
                JSONObject tag = tags.getJSONObject(t);
                String key = tag.optString("key");
                String value = tag.optString("value");
                
                if ("http.method".equals(key)) { hasHttpMethod = true; httpMethod = value; }
                if ("http.url".equals(key) || "http.target".equals(key)) { hasHttpUrl = true; httpUrl = value; }
            }
        }
        
        // Must be an actual API call
        if (hasHttpMethod && hasHttpUrl && (httpUrl.contains("/api/") || httpMethod.matches("GET|POST|PUT|DELETE|PATCH"))) {
            return true;
        }
        
        if (opName.contains("/api/") && (opName.contains("GET") || opName.contains("POST") || opName.contains("PUT") || opName.contains("DELETE") || opName.contains("PATCH"))) {
            return true;
        }
        
        return opName.matches("(GET|POST|PUT|DELETE|PATCH)\\s+/api/.*");
    }

    private static String getServiceName(JSONObject span, JSONObject processes) {
        String processId = span.optString("processID", "");
        if (processes != null && processes.has(processId)) {
            return processes.getJSONObject(processId).optString("serviceName", "unknown-service");
        }
        return "unknown-service";
    }

    private static String[] extractHttpInfo(JSONObject span) {
        String method = "";
        String endpoint = "";
        String status = "";
        String opName = span.optString("operationName", "");
        
        JSONArray tags = span.optJSONArray("tags");
        if (tags != null) {
            for (int t = 0; t < tags.length(); t++) {
                JSONObject tag = tags.getJSONObject(t);
                String key = tag.optString("key");
                String value = tag.optString("value");
                
                if ("http.method".equals(key)) method = value;
                else if ("http.url".equals(key) || "http.target".equals(key)) endpoint = value;
                else if ("http.status_code".equals(key)) status = value;
            }
        }
        
        // Extract from operation name if tags don't have the info
        if (method.isEmpty() && opName.matches("(GET|POST|PUT|DELETE|PATCH)\\s+.*")) {
            String[] parts = opName.split("\\s+", 2);
            if (parts.length == 2) {
                method = parts[0];
                endpoint = parts[1];
            }
        }
        
        if (endpoint.isEmpty()) endpoint = opName;
        if (status.isEmpty()) status = "?";
        
        return new String[]{method, endpoint, status};
    }

    private static boolean hasErrorTag(JSONObject span) {
        if (!span.has("tags")) return false;
        
        JSONArray tags = span.getJSONArray("tags");
        for (int i = 0; i < tags.length(); i++) {
            JSONObject tag = tags.getJSONObject(i);
            if ("error".equals(tag.optString("key")) && 
                "bool".equals(tag.optString("type")) && 
                tag.optBoolean("value", false)) {
                return true;
            }
        }
        return false;
    }

    private static String formatDuration(long durationMicros) {
        if (durationMicros < 1000) {
            return durationMicros + "μs";
        } else if (durationMicros < 1000000) {
            return String.format("%.1f", durationMicros / 1000.0) + "ms";
        } else {
            return String.format("%.2f", durationMicros / 1000000.0) + "s";
        }
    }

    private static void renderApiHierarchy(StringBuilder table, String spanId, Map<String, JSONObject> apiSpanById, Map<String, List<String>> apiChildren, JSONObject processes, int depth, AtomicInteger counter, String prefix) {
        JSONObject span = apiSpanById.get(spanId);
        if (span == null) return;
        
        // Extract API information
        String serviceName = getServiceName(span, processes);
        String[] httpInfo = extractHttpInfo(span);
        String method = httpInfo[0];
        String endpoint = httpInfo[1];
        String status = httpInfo[2];
        long duration = span.optLong("duration", 0L);
        
        // Filter out gateway services - only show actual business APIs
        boolean isGateway = serviceName.toLowerCase().contains("gateway") || 
                           serviceName.toLowerCase().contains("proxy") ||
                           serviceName.toLowerCase().equals("gateway-service") ||
                           serviceName.toLowerCase().equals("ts-gateway-service") ||
                           serviceName.toLowerCase().equals("api-gateway");
        
        // If this is a gateway, skip rendering but still process children
        if (isGateway) {
            // Render children recursively without showing this gateway span
            List<String> children = apiChildren.getOrDefault(spanId, Collections.emptyList());
            for (String childId : children) {
                renderApiHierarchy(table, childId, apiSpanById, apiChildren, processes, depth, counter, "");
            }
            return;
        }
        
        // Check for errors in this span
        boolean hasError = hasErrorTag(span);
        String durationStr = formatDuration(duration);
        
        // Use SINGLE status indicator - prioritize error detection over HTTP status
        String statusIcon;
        if (hasError) {
            statusIcon = "❌";  // Error detected in span
        } else if (status.startsWith("2")) {
            statusIcon = "✅";  // HTTP 2xx success
        } else if (status.startsWith("4") || status.startsWith("5")) {
            statusIcon = "❌";  // HTTP 4xx/5xx error
        } else {
            statusIcon = "❓";  // Unknown/other status
        }
        
        // Smart service name handling - keep essential info
        String displayService = serviceName;
        if (serviceName.startsWith("ts-")) {
            // Keep meaningful part: ts-admin-order-service -> admin-order
            displayService = serviceName.substring(3);
            if (displayService.endsWith("-service")) {
                displayService = displayService.substring(0, displayService.length() - 8);
            }
        }
        
        // Smart endpoint handling - preserve important path info
        String displayEndpoint = endpoint;
        if (endpoint.startsWith("http")) {
            try {
                // Extract path from full URL: http://host:port/path -> /path
                int pathStart = endpoint.indexOf("/", 8);
                if (pathStart > 0) {
                    displayEndpoint = endpoint.substring(pathStart);
                }
            } catch (Exception ignored) {
                // If parsing fails, use original
            }
        }
        
        // Compress common patterns but keep essential info
        if (displayEndpoint.contains("/api/v1/")) {
            displayEndpoint = displayEndpoint.replace("/api/v1/", "/v1/");
        }
        
        // Create compact tree line
        StringBuilder line = new StringBuilder();
        
        // Minimal indentation for space efficiency
        for (int i = 0; i < depth; i++) {
            line.append("  ");  // 2 spaces per level
        }
        
        // Add compact connector
        if (depth > 0) {
            line.append("├─");
        }
        
        // Build comprehensive format with full information
        int apiNumber = counter.getAndIncrement();
        
        // Main line: [#] Status Service Method HTTPStatus(time)
        line.append(String.format("[%d] %s %s %s %s(%s)", 
            apiNumber, statusIcon, displayService, method, status, durationStr));
        table.append(line.toString()).append("\n");
        
        // Detail line: Full endpoint path (indented)
        StringBuilder detailLine = new StringBuilder();
        for (int i = 0; i < depth; i++) {
            detailLine.append("  ");
        }
        if (depth > 0) {
            detailLine.append("   ");  // Extra indent for detail line
        } else {
            detailLine.append("    ");  // Indent for root detail line
        }
        detailLine.append("↳ ").append(endpoint);
        table.append(detailLine.toString()).append("\n");
        
        // Render children recursively
        List<String> children = apiChildren.getOrDefault(spanId, Collections.emptyList());
        for (String childId : children) {
            renderApiHierarchy(table, childId, apiSpanById, apiChildren, processes, depth + 1, counter, "");
        }
    }

    private static void analyzeAndRecordParameterErrors(JSONObject trace, Map<String, String> stepParameters) {
        try {
            // Get LLM properties from system properties
            Map<String, String> llmProperties = new HashMap<>();
            llmProperties.put("llm.enabled", System.getProperty("llm.enabled", "true"));
            llmProperties.put("llm.model.type", System.getProperty("llm.model.type", "ollama"));
            llmProperties.put("llm.ollama.enabled", System.getProperty("llm.ollama.enabled", "true"));
            llmProperties.put("llm.ollama.url", System.getProperty("llm.ollama.url", "http://localhost:11434"));
            llmProperties.put("llm.ollama.model", System.getProperty("llm.ollama.model", "gemma3:4b"));
            llmProperties.put("llm.gemini.enabled", System.getProperty("llm.gemini.enabled", "false"));
            llmProperties.put("llm.gemini.api.key", System.getProperty("llm.gemini.api.key", ""));
            llmProperties.put("llm.gemini.model", System.getProperty("llm.gemini.model", "gemini-2.0-flash-exp"));
            llmProperties.put("llm.gemini.api.url", System.getProperty("llm.gemini.api.url", "https://generativelanguage.googleapis.com/v1beta/models"));
            
            // Analyze parameter errors
            es.us.isa.restest.inputs.smart.ParameterErrorAnalyzer.ParameterErrorAnalysisResult result = 
                es.us.isa.restest.inputs.smart.ParameterErrorAnalyzer.analyzeParameterErrors(trace, stepParameters, llmProperties);
            
            if (result.hasParameterErrors()) {
                System.out.println("🔍 Parameter Error Analysis: Found " + result.getIdentifiedErrors().size() + " parameter-related errors");
                
                // Load and update the input fetch registry
                String registryPath = System.getProperty("smart.input.fetch.registry.path");
                if (registryPath != null && !registryPath.isEmpty()) {
                    try {
                        java.io.File registryFile = new java.io.File(registryPath);
                        es.us.isa.restest.inputs.smart.InputFetchRegistry registry;
                        
                        if (registryFile.exists()) {
                            registry = es.us.isa.restest.inputs.smart.InputFetchRegistry.loadFromFile(registryFile);
                        } else {
                            registry = new es.us.isa.restest.inputs.smart.InputFetchRegistry();
                        }
                        
                        // Record each parameter error
                        for (es.us.isa.restest.inputs.smart.ParameterError error : result.getIdentifiedErrors()) {
                            registry.addParameterError(error.getApiEndpoint(), error.getParameterName(), error);
                            System.out.println("📝 Recorded error: " + error.getParameterName() + " -> " + error.getErrorType() + ": " + error.getErrorReason());
                        }
                        
                        // Save updated registry
                        registry.saveToFile(registryFile);
                        System.out.println("💾 Updated parameter error registry at: " + registryPath);
                        
                    } catch (Exception e) {
                        System.err.println("⚠️ Failed to update parameter error registry: " + e.getMessage());
                        e.printStackTrace();
                    }
                } else {
                    System.err.println("⚠️ Parameter error registry path not configured. Set 'smart.input.fetch.registry.path' property.");
                }
            } else {
                System.out.println("✅ No parameter-related errors detected in trace");
            }
            
        } catch (Exception e) {
            System.err.println("⚠️ Error during parameter error analysis: " + e.getMessage());
            e.printStackTrace();
        }
    }

    @BeforeClass
    public static void setupRestAssured() {
        RestAssured.baseURI = "http://129.62.148.112:32677";
        // Configure Allure results directory
        System.setProperty("allure.results.directory", "target/allure-results");
        // ⚠️ AllureRestAssured filter disabled - causes duplicate request logging
        // We use manual, controlled attachments instead for cleaner reports
        // RestAssured.filters(new AllureRestAssured());
    }

    @Test
    public void test_negative_POST_101_1() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price, id, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"cad305dd-b434-4d43-91fd-bccf9a7bdb16\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"123\",\"accountId\":\"ACC001\",\"contactsName\"...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=US987654321, price=150.00, id=AR-7891, trainNumber=G5678}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_1");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "US123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "chengdu", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "123", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_1");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelTime=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelTime=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelTime=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US987654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"chengdu\",\"id\":\"AR-7891\",\"price\":\"150.00\",\"seatClass\":1,\"seatNumber\":\"3F\",\"status\":5,\"to\":\"Beijing\",\"trainNumber\":\"G5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":123}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US987654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"chengdu\",\"id\":\"AR-7891\",\"price\":\"150.00\",\"seatClass\":1,\"seatNumber\":\"3F\",\"status\":5,\"to\":\"Beijing\",\"trainNumber\":\"G5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":123}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelTime", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_1",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_1");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_1",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_1");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_1");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_1");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_2() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_2");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "abc", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "512.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_2");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "documentType=abc");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "documentType=abc\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("documentType=abc");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"abc\",\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"512.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"abc\",\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"512.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("documentType", "abc");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_2",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_2");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_2",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_2");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_2");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_2");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_3() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, seatClass, price]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"7b0dbabc-f08d-4e8f-8ffb-417be16456c8\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, documentType=2, seatClass=3, price=500.0}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_3");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-25.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "TS-12345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "1024.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_3");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsDocumentNumber=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsDocumentNumber=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsDocumentNumber=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":123,\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"TS-12345\",\"price\":\"500.0\",\"seatClass\":5,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":123,\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"TS-12345\",\"price\":\"500.0\",\"seatClass\":5,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsDocumentNumber", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_3",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_3");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_3",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_3");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_3");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_3");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_4() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_4");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "0.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_4");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsName=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsName=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsName=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":123,\"differenceMoney\":\"0.00\",\"documentType\":4,\"from\":\"shenzhen\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"Guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":123,\"differenceMoney\":\"0.00\",\"documentType\":4,\"from\":\"shenzhen\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"Guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsName", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_4",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_4");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_4",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_4");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_4");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_4");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_5() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"108439dc-ceb3-45fa-aa9b-1fcc16ce8bcc\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=20B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_5");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "256.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_5");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "differenceMoney=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "differenceMoney=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("differenceMoney=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":123,\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"256.0\",\"seatClass\":1,\"seatNumber\":\"20B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":123,\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"256.0\",\"seatClass\":1,\"seatNumber\":\"20B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("differenceMoney", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_5",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_5");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_5",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_5");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_5");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_5");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_6() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_6");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_6");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatNumber=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatNumber=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatNumber=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":123,\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":123,\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatNumber", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_6",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_6");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_6",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_6");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_6");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_6");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_7() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"f2167b95-52dd-4107-a106-866b67fec275\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"123\",\"contactsNam...\nEnhanced Parameters: {from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=05B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_7");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "8.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_7");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "accountId=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "accountId=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("accountId=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":123,\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"8.0\",\"seatClass\":2,\"seatNumber\":\"05B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":123,\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"8.0\",\"seatClass\":2,\"seatNumber\":\"05B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("accountId", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_7",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_7");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_7",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_7");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_7");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_7");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_8() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_8");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC002", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "abc", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Maria Garcia", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "nanjing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_8");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "coachNumber=abc");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "coachNumber=abc\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("coachNumber=abc");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":\"abc\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"nanjing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":\"abc\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"nanjing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("coachNumber", "abc");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_8",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_8");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_8",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_8");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_8");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_8");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_9() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, to, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"120add73-bf67-45f2-8895-5cb55309bf40\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"20260103\",\"travelTime\":\"18:20:00\",\"accountId\":\"ACC001\",\"contactsNa...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD9876543, from=Shanghai, to=Guangzhou, trainNumber=D5678}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_9");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ADMIN-001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "20260103", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_9");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelDate=20260103");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelDate=20260103\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelDate=20260103");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"ADMIN-001\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":20260103,\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"ADMIN-001\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":20260103,\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelDate", "20260103");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_9",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_9");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_9",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_9");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_9");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_9");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_10() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"6d1e4a8b-a4ae-4e64-9ec6-8aee8d8eb3ff\",\"boughtDate\":\"123456789\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsN...\nEnhanced Parameters: {from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=12A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_10");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "8", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "125.75", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_10");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "boughtDate=123456789");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "boughtDate=123456789\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("boughtDate=123456789");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":123456789,\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":3,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":123456789,\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":3,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("boughtDate", "123456789");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_10",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_10");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_10",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_10");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_10");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_10");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_11() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"a64f61d4-6514-4043-a1c7-c4a5d1d1b4c9\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD2345678, trainNumber=D9012, seatNumber=30C}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_11");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "25B", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "D5678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_11");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "price=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "price=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("price=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD2345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":123,\"seatClass\":1,\"seatNumber\":\"30C\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"D9012\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD2345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":123,\"seatClass\":1,\"seatNumber\":\"30C\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"D9012\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("price", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_11",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_11");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_11",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_11");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_11");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_11");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_12() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_12");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "15", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-10.25", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "abc", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_12");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatClass=abc");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatClass=abc\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatClass=abc");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":\"abc\",\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":\"abc\",\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatClass", "abc");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_12",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_12");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_12",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_12");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_12");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_12");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_13() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price, id, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"a6253636-710f-44a4-8514-502b836c9818\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\",\"accountId\":\"ACC001\",\"contactsName\":\"John Sm...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=JP123456789012346, price=250.00, id=AR-7891, trainNumber=C9013, seatNumber=12B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_13");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "JP123456789012345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "249.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "C9012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "22:45:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_13");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "from=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "from=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("from=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP123456789012346\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":123,\"id\":\"AR-7891\",\"price\":\"250.00\",\"seatClass\":5,\"seatNumber\":\"12B\",\"status\":1,\"to\":\"Hangzhou\",\"trainNumber\":\"C9013\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP123456789012346\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":123,\"id\":\"AR-7891\",\"price\":\"250.00\",\"seatClass\":5,\"seatNumber\":\"12B\",\"status\":1,\"to\":\"Hangzhou\",\"trainNumber\":\"C9013\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("from", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_13",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_13");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_13",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_13");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_13");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_13");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_14() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, trainNumber, to]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"92f0cb60-f0ba-4fa9-9f11-ae587e38afda\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=AB1234568, from=Shanghai, trainNumber=G1235, to=Guangzhou}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_14");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_14");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "id=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "id=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("id=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234568\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":2,\"from\":\"Shanghai\",\"id\":123,\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"G1235\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234568\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":2,\"from\":\"Shanghai\",\"id\":123,\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"G1235\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("id", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_14",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_14");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_14",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_14");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_14");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_14");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_15() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [contactsDocumentNumber, price, id]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"9514f959-11d0-43bf-90fa-ef7fbee4f825\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {contactsDocumentNumber=GB1234567890123456789, price=129.99, id=AR-7890-01}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_15");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "GB12345678901234567890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "7C", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_15");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "to=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "to=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("to=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890-01\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":4,\"to\":123,\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890-01\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":4,\"to\":123,\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("to", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_15",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_15");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_15",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_15");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_15");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_15");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_16() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_16");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Robert Johnson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-25.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ROUTE-456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "89.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "18D", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_16");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"89.00\",\"seatClass\":1,\"seatNumber\":\"18D\",\"status\":5,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"89.00\",\"seatClass\":1,\"seatNumber\":\"18D\",\"status\":5,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_16",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_16");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_16",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_16");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_16");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_16");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_17() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [documentType, contactsDocumentNumber, price]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"e5b64fca-a92d-4ff6-8533-23db24e68145\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\",\"accountId\":\"ACC001\",\"contactsName\":\"John Sm...\nEnhanced Parameters: {documentType=1, contactsDocumentNumber=CN12345678901234567, price=129.99}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_17");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "CN123456789012345678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "SERVICE-789012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_17");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "trainNumber=123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "trainNumber=123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("trainNumber=123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"CN12345678901234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"SERVICE-789012\",\"price\":\"129.99\",\"seatClass\":2,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":123,\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"CN12345678901234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"SERVICE-789012\",\"price\":\"129.99\",\"seatClass\":2,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":123,\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("trainNumber", "123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_17",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_17");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_17",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_17");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_17");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_17");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_18() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_18");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "US123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Emily Davis", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "399.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_18");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"399.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"399.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_18",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_18");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_18",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_18");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_18");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_18");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_19() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_19");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "0.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "59.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "active", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "T3456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_19");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "status=active");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "status=active\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("status=active");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":4,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"59.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":\"active\",\"to\":\"Beijing\",\"trainNumber\":\"T3456\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":4,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"59.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":\"active\",\"to\":\"Beijing\",\"trainNumber\":\"T3456\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("status", "active");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_19",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_19");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_19",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_19");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_19");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_19");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_20() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"facecb2d-fb02-40f0-b72b-a21aed54c5ee\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"fake\",\"accountId\":\"ACC001\",\"contactsName...\nEnhanced Parameters: {price=2.0, from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=25B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_20");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "125.75", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "2.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "25B", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-20", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "fake", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_20");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelTime=fake");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelTime=fake\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelTime=fake");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"2.0\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"fake\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"2.0\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"fake\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelTime", "fake");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_20",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_20");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_20",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_20");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_20");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_20");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_21() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [travelTime, documentType, contactsDocumentNumber, contactsName, differenceMoney, seatNumber, accountId, coachNumber, travelDate, boughtDate, price, seatClass, from, id, to, trainNumber, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"3e0ca115-9ddd-4faf-baff-c91406f76bbe\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {travelTime=08:30:00, documentType=-999999, contactsDocumentNumber=AB1234567, contactsName=Michael Wilson, differenceMoney=50.00, seatNumber=12A, accountId=ACC001, coachNumber=12, travelDate=2026-01-15, boughtDate=2026-01-15, price=129.99, seatClass=1, from=Beijing, id=AR-7890, to=Beijing, trainNumber=G1234, status=2}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_21");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Michael Wilson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "-999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_21");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters (ENHANCED)", "documentType=-999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "documentType=-999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("documentType=-999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":\"-999999\",\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":\"-999999\",\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("documentType", "-999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_21",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_21");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_21",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_21");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_21");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_21");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_22() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"404ea862-5c0f-459a-9404-df7f476302d2\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, price=0.00, from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=20B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_22");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "fake123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-10.25", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_22");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsDocumentNumber=fake123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsDocumentNumber=fake123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsDocumentNumber=fake123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"fake123\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":1,\"seatNumber\":\"20B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"fake123\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":1,\"seatNumber\":\"20B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsDocumentNumber", "fake123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_22",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_22");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_22",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_22");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_22");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_22");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_23() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"520da053-cad6-44ed-b767-1972805b8d1a\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD7654321, from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=23B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_23");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "FakeName123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "TS-12345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "4.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_23");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsName=FakeName123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsName=FakeName123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsName=FakeName123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"FakeName123\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"TS-12345\",\"price\":\"4.0\",\"seatClass\":3,\"seatNumber\":\"23B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"FakeName123\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"TS-12345\",\"price\":\"4.0\",\"seatClass\":3,\"seatNumber\":\"23B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsName", "FakeName123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_23",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_23");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_23",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_23");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_23");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_23");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_24() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [travelTime, documentType, contactsDocumentNumber, contactsName, seatNumber, accountId, coachNumber, travelDate, seatClass, price, boughtDate, from, to, trainNumber, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"90e1eaaf-ba7c-4302-92b5-5a906e9ae14a\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC003\",\"contacts...\nEnhanced Parameters: {travelTime=08:30:00, documentType=1, contactsDocumentNumber=JP123456789012345, contactsName=John Smith, seatNumber=12A, accountId=ACC003, coachNumber=4, travelDate=2026-01-15, seatClass=4, price=129.99, boughtDate=2026-01-15, from=Beijing, to=Shanghai, trainNumber=G1234, status=1}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_24");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC003", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "JP123456789012345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "null", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_24");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "differenceMoney=null");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "differenceMoney=null\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("differenceMoney=null");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC003\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"null\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC003\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"null\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("differenceMoney", "null");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_24",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_24");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_24",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_24");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_24");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_24");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_25() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_25");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "8", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "GB12345678901234567890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "EMPTY", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_25");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatNumber=EMPTY");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatNumber=EMPTY\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatNumber=EMPTY");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"GB12345678901234567890\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"EMPTY\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"GB12345678901234567890\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"EMPTY\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatNumber", "EMPTY");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_25",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_25");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_25",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_25");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_25");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_25");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_26() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [coachNumber, travelDate, documentType, boughtDate, price, seatClass, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"5a9388fe-3ff7-460c-a4fd-390145e1d878\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"fakeaccount123\",\"...\nEnhanced Parameters: {coachNumber=-12, travelDate=2023-01-15, documentType=0, boughtDate=2023-01-15, price=-16.0, seatClass=0, seatNumber=12}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_26");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "fakeaccount123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ADMIN-001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "16.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_26");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "accountId=fakeaccount123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "accountId=fakeaccount123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("accountId=fakeaccount123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"fakeaccount123\",\"boughtDate\":\"2023-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"-16.0\",\"seatClass\":1,\"seatNumber\":\"12\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2023-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"fakeaccount123\",\"boughtDate\":\"2023-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"-16.0\",\"seatClass\":1,\"seatNumber\":\"12\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2023-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("accountId", "fakeaccount123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_26",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_26");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_26",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_26");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_26");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_26");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_27() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"b3f01c03-7443-4adc-bfb7-5f275c591c0b\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsName\":\"Maria G...\nEnhanced Parameters: {price=299.0, from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=05B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_27");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "-999999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Maria Garcia", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "128.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "E7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_27");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "coachNumber=-999999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "coachNumber=-999999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("coachNumber=-999999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":\"-999999999\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"299.0\",\"seatClass\":1,\"seatNumber\":\"05B\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":\"-999999999\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"299.0\",\"seatClass\":1,\"seatNumber\":\"05B\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("coachNumber", "-999999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_27",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_27");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_27",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_27");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_27");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_27");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_28() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [boughtDate, contactsDocumentNumber, from, to, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"1d440cac-26fe-4f75-abec-c9d0c06c257c\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2025-02-30\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {boughtDate=2025-01-15, contactsDocumentNumber=CD1234567, from=Shanghai, to=Guangzhou, trainNumber=D1234}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_28");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "15", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-25.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2025-02-30", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_28");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelDate=2025-02-30");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelDate=2025-02-30\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelDate=2025-02-30");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2025-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"CD1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":4,\"to\":\"Guangzhou\",\"trainNumber\":\"D1234\",\"travelDate\":\"2025-02-30\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2025-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"CD1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":4,\"to\":\"Guangzhou\",\"trainNumber\":\"D1234\",\"travelDate\":\"2025-02-30\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelDate", "2025-02-30");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_28",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_28");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_28",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_28");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_28");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_28");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_29() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, contactsDocumentNumber, seatClass, price, from, to, trainNumber, seatNumber, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"41c91e35-845a-468e-a07b-67744bb4f933\",\"boughtDate\":\"2025-02-30\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, documentType=2, contactsDocumentNumber=CD9876543, seatClass=2, price=159.99, from=Shanghai, to=Guangzhou, trainNumber=G1234, seatNumber=8D, status=6}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_29");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2025-02-30", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "7C", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "D5678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_29");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "boughtDate=2025-02-30");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "boughtDate=2025-02-30\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("boughtDate=2025-02-30");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2025-02-30\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"159.99\",\"seatClass\":1,\"seatNumber\":\"8D\",\"status\":5,\"to\":\"Guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2025-02-30\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"159.99\",\"seatClass\":1,\"seatNumber\":\"8D\",\"status\":5,\"to\":\"Guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("boughtDate", "2025-02-30");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_29",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_29");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_29",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_29");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_29");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_29");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_30() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_30");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-14", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "CN123456789012345678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "fake", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_30");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "price=fake");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "price=fake\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("price=fake");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN123456789012345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Guangzhou\",\"id\":\"AR-7890\",\"price\":\"fake\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN123456789012345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Guangzhou\",\"id\":\"AR-7890\",\"price\":\"fake\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("price", "fake");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_30",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_30");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_30",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_30");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_30");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_30");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_31() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"9da54fae-2b1b-40fa-8ab1-edaafeb75656\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=12A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_31");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "US123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "32.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "-999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_31");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatClass=-999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatClass=-999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatClass=-999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"32.0\",\"seatClass\":\"-999\",\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"32.0\",\"seatClass\":\"-999\",\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatClass", "-999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_31",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_31");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_31",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_31");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_31");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_31");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_32() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"8653e1c4-4af2-49f8-bb47-718003ec37ae\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD2345678, price=0.00, seatNumber=00A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_32");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Robert Johnson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "FakeStationName123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "18D", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_32");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "from=FakeStationName123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "from=FakeStationName123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("from=FakeStationName123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD2345678\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"FakeStationName123\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":5,\"seatNumber\":\"00A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD2345678\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"FakeStationName123\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":5,\"seatNumber\":\"00A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("from", "FakeStationName123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_32",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_32");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_32",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_32");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_32");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_32");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_33() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_33");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_33");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_33",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_33");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_33",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_33");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_33");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_33");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_34() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"78064cb8-7405-488f-89ff-4ba28b838b8f\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC004\",\"contacts...\nEnhanced Parameters: {accountId=ACC001, contactsDocumentNumber=AB123456789, price=0.00}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_34");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC004", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "fake123", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-20", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_34");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "id=fake123");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "id=fake123\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("id=fake123");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"AB123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shenzhen\",\"id\":\"fake123\",\"price\":\"0.00\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"AB123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shenzhen\",\"id\":\"fake123\",\"price\":\"0.00\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("id", "fake123");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_34",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_34");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_34",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_34");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_34");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_34");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_35() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"45511301-1bcc-44c2-965f-3eb941131abc\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC005\",\"contacts...\nEnhanced Parameters: {accountId=ACC001, contactsDocumentNumber=AB1234568, trainNumber=G1235, seatNumber=13B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_35");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC005", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Emily Davis", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "0.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "fake", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_35");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "to=fake");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "to=fake\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("to=fake");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234568\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"0.00\",\"documentType\":2,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"13B\",\"status\":1,\"to\":\"fake\",\"trainNumber\":\"G1235\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234568\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"0.00\",\"documentType\":2,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"13B\",\"status\":1,\"to\":\"fake\",\"trainNumber\":\"G1235\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("to", "fake");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_35",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_35");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_35",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_35");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_35");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_35");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_36() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"84b23059-3266-47ec-b6f2-13fef0bbbd69\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\",\"accountId\":\"ACC001\",\"contactsName\":\"John Sm...\nEnhanced Parameters: {price=299.00, from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=12B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_36");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "125.75", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ROUTE-456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "fake", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "22:45:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_36");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters (ENHANCED)", "trainNumber=G1234");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "trainNumber=fake\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("trainNumber=fake");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"299.00\",\"seatClass\":1,\"seatNumber\":\"12B\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"299.00\",\"seatClass\":1,\"seatNumber\":\"12B\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("trainNumber", "fake");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_36",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_36");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_36",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_36");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_36");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_36");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_37() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [travelTime, documentType, contactsDocumentNumber, contactsName, differenceMoney, seatNumber, accountId, coachNumber, travelDate, boughtDate, price, seatClass, from, id, to, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"777f467c-74c0-4b09-a01a-e6745ebdac8f\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"9j7f91h2-26hg-9hk...\nEnhanced Parameters: {travelTime=08:30:00, documentType=1, contactsDocumentNumber=AB1234567, contactsName=Michael Wilson, differenceMoney=50.00, seatNumber=12A, accountId=9j7f91h2-26hg-9hk6-g0gg-g1395bi, coachNumber=12, travelDate=2026-01-15, boughtDate=2026-01-15, price=64.0, seatClass=1, from=Hangzhou, id=SERVICE-789012, to=Beijing, trainNumber=G1234}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_37");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "9j7f91h2-26hg-9hk6-g0gg-g1395bi", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Michael Wilson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "SERVICE-789012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "64.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "-999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_37");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "status=-999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "status=-999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("status=-999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"9j7f91h2-26hg-9hk6-g0gg-g1395bi\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Hangzhou\",\"id\":\"SERVICE-789012\",\"price\":\"64.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":\"-999999\",\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"9j7f91h2-26hg-9hk6-g0gg-g1395bi\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Hangzhou\",\"id\":\"SERVICE-789012\",\"price\":\"64.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":\"-999999\",\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("status", "-999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_37",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_37");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_37",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_37");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_37");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_37");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_38() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"d9e91470-6a04-4b1f-bc8d-7dba786e457b\",\"boughtDate\":\"2026-02-28\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA...\nEnhanced Parameters: {accountId=7h5d79f0-04fe-4844-e8ee-e91739g2gd9i, documentType=2, seatClass=2, status=2}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_38");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "7h5d79f0-04fe-7fi4-e8ee-e91739g2gd9i", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-10.25", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "C9012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_38");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelTime=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelTime=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelTime=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"7h5d79f0-04fe-4844-e8ee-e91739g2gd9i\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"C9012\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"7h5d79f0-04fe-4844-e8ee-e91739g2gd9i\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"C9012\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelTime", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_38",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_38");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_38",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_38");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_38");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_38");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_39() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_39");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "9999999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_39");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "documentType=9999999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "documentType=9999999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("documentType=9999999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"9999999999\",\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"9999999999\",\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("documentType", "9999999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_39",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_39");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_39",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_39");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_39");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_39");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_40() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"df44102f-4ffa-4071-b1cb-b0f052ab2e93\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsName\":\"Maria G...\nEnhanced Parameters: {accountId=ACC002, documentType=2, seatClass=2, status=2}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_40");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Maria Garcia", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "T3456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_40");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsDocumentNumber=1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsDocumentNumber=1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsDocumentNumber=1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"T3456\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"T3456\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsDocumentNumber", "1234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_40",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_40");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_40",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_40");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_40");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_40");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_41() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"47914df3-1dc4-4b99-9560-b99c4f673d48\",\"boughtDate\":\"2026-01-08\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\",\"accountId\":\"ACC001\",\"contactsName\":\"AAAAAAA...\nEnhanced Parameters: {accountId=ACC002, documentType=2, seatClass=2, status=2}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_41");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "E7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_41");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsName=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsName=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsName=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"E7890\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"E7890\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsName", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_41",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_41");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_41",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_41");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_41");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_41");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_42() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"669f27c7-9401-4c0d-9778-3e6a84d1dc65\",\"boughtDate\":\"2026-02-14\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=12A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_42");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-14", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Robert Johnson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "512.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_42");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "differenceMoney=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "differenceMoney=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("differenceMoney=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"512.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"512.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("differenceMoney", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_42",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_42");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_42",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_42");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_42");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_42");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_43() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_43");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "5f3b57d8-82dc-5dg2-c6cc-c79517e0eb7g", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "TS-12345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_43");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatNumber=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatNumber=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatNumber=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"5f3b57d8-82dc-5dg2-c6cc-c79517e0eb7g\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Beijing\",\"id\":\"TS-12345\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"5f3b57d8-82dc-5dg2-c6cc-c79517e0eb7g\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Beijing\",\"id\":\"TS-12345\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatNumber", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_43",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_43");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_43",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_43");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_43");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_43");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_44() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_44");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "8", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Emily Davis", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ADMIN-001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "1024.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_44");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "accountId=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "accountId=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("accountId=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"1024.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"1024.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("accountId", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_44",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_44");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_44",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_44");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_44");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_44");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_45() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_45");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "6g4c68e9-93ed-6eh3-d7dd-d80628f1fc8h", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "9999999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "JP123456789012345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_45");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "coachNumber=9999999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "coachNumber=9999999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("coachNumber=9999999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"6g4c68e9-93ed-6eh3-d7dd-d80628f1fc8h\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":\"9999999999\",\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":4,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"6g4c68e9-93ed-6eh3-d7dd-d80628f1fc8h\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":\"9999999999\",\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":4,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("coachNumber", "9999999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_45",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_45");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_45",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_45");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_45");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_45");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_46() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, contactsDocumentNumber, price, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"5812d9de-2b05-4dc5-a5c0-787c43deeb66\",\"boughtDate\":\"2026-03-10\",\"travelDate\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA...\nEnhanced Parameters: {accountId=ACC002, documentType=1, contactsDocumentNumber=GB1234567890123456789, price=256.00, seatClass=1, status=1}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_46");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "GB12345678901234567890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "256.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_46");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelDate=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelDate=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelDate=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"256.00\",\"seatClass\":2,\"seatNumber\":\"12A\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"256.00\",\"seatClass\":2,\"seatNumber\":\"12A\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelDate", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_46",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_46");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_46",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_46");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_46");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_46");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_47() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, coachNumber, documentType, price, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"23da27e2-36a1-42ae-ae14-069d4d82ad20\",\"boughtDate\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA...\nEnhanced Parameters: {accountId=ACC002, coachNumber=1, documentType=2, price=99.99, seatClass=2, status=1}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_47");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_47");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "boughtDate=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "boughtDate=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("boughtDate=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"99.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":4,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"99.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":4,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("boughtDate", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_47",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_47");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_47",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_47");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_47");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_47");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_48() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_48");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "15", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-25.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ROUTE-456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_48");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "price=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "price=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("price=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("price", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_48",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_48");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_48",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_48");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_48");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_48");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_49() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_49");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "SERVICE-789012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_49");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"SERVICE-789012\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Hangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"SERVICE-789012\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Hangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_49",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_49");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_49",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_49");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_49");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_49");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_50() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_50");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "8i6e80g1-15gf-8gj5-f9ff-f0284ah3he0j", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "9999999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_50");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatClass=9999999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatClass=9999999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatClass=9999999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"8i6e80g1-15gf-8gj5-f9ff-f0284ah3he0j\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":\"9999999999\",\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"8i6e80g1-15gf-8gj5-f9ff-f0284ah3he0j\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":\"9999999999\",\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatClass", "9999999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_50",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_50");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_50",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_50");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_50");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_50");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_51() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [documentType, price, differenceMoney, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"cf944ea7-4497-4443-bbd0-f310852e90a2\",\"boughtDate\":\"2026-02-14\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"4d2a46c7-71cb-4cf...\nEnhanced Parameters: {documentType=0, price=0.00, differenceMoney=-10.00, status=0}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_51");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-14", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Michael Wilson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "25B", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_51");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "from=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "from=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("from=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"-10.00\",\"documentType\":2,\"from\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":4,\"seatNumber\":\"25B\",\"status\":5,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"-10.00\",\"documentType\":2,\"from\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":4,\"seatNumber\":\"25B\",\"status\":5,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("from", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_51",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_51");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_51",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_51");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_51");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_51");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_52() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"2fec689a-8040-49ab-a88a-a3af67f1c5ba\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {price=8.0, from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=7C}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_52");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "8.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "7C", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_52");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "id=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "id=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("id=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"price\":\"8.0\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"price\":\"8.0\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("id", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_52",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_52");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_52",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_52");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_52");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_52");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_53() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price, id, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"6a054fa0-3fe8-491c-b984-8e392c0da386\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"22:45:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD7654321, price=150.00, id=AR-7891, trainNumber=G5678}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_53");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-20", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "22:45:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_53");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "to=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "to=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("to=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7891\",\"price\":\"150.00\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"trainNumber\":\"G5678\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"22:45:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7891\",\"price\":\"150.00\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\",\"trainNumber\":\"G5678\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"22:45:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("to", "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_53",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_53");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_53",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_53");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_53");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_53");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_54() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_54");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "CN123456789012345678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "TS-12345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "18D", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_54");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "trainNumber=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "trainNumber=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("trainNumber=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN123456789012345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"TS-12345\",\"price\":\"129.99\",\"seatClass\":5,\"seatNumber\":\"18D\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN123456789012345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"TS-12345\",\"price\":\"129.99\",\"seatClass\":5,\"seatNumber\":\"18D\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("trainNumber", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_54",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_54");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_54",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_54");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_54");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_54");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_55() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_55");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC002", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "US123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ADMIN-001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "9999999999", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_55");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "status=9999999999");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "status=9999999999\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("status=9999999999");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":\"9999999999\",\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":\"9999999999\",\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("status", "9999999999");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_55",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_55");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_55",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_55");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_55");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_55");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_56() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, contactsDocumentNumber, price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"067e5aaf-4461-4c57-a119-189b8ec83bd3\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"\",\"accountId\":\"ACC001\",\"contactsName\":\"J...\nEnhanced Parameters: {accountId=ACC002, documentType=1, contactsDocumentNumber=XY9876543, price=89.50, from=shanghai, to=guangzhou, trainNumber=G1234, seatNumber=12A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_56");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "0.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "suzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "D5678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_56");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelTime=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelTime=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelTime=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"XY9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":3,\"from\":\"shanghai\",\"id\":\"AR-7890\",\"price\":\"89.50\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"XY9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":3,\"from\":\"shanghai\",\"id\":\"AR-7890\",\"price\":\"89.50\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":2,\"to\":\"guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelTime", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_56",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_56");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_56",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_56");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_56");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_56");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_57() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_57");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC003", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "wuhan", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "C9012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_57");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "documentType=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "documentType=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("documentType=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC003\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"\",\"from\":\"wuhan\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"C9012\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC003\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"\",\"from\":\"wuhan\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"C9012\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("documentType", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_57",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_57");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_57",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_57");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_57");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_57");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_58() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, seatClass, price, from, to, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"388e5179-dbc9-4d42-9eea-1dbd7027cb66\",\"boughtDate\":\"2026-02-28\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC004\",\"contactsName\":\"John Sm...\nEnhanced Parameters: {accountId=ACC001, documentType=1, seatClass=2, price=299.99, from=Shanghai, to=Guangzhou, trainNumber=T1234}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_58");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC004", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "T3456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_58");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsDocumentNumber=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsDocumentNumber=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsDocumentNumber=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":4,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"299.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"T1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":4,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"299.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"T1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsDocumentNumber", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_58",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_58");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_58",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_58");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_58");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_58");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_59() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_59");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "25B", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_59");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsName=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsName=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsName=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsName", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_59",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_59");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_59",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_59");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_59");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_59");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_60() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, id, to, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"489c22ae-319d-47ab-9fda-7193d36d97c1\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=JP987654321098765, from=Shanghai, id=AR-7891, to=Guangzhou, trainNumber=D5678}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_60");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "JP123456789012345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_60");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "differenceMoney=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "differenceMoney=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("differenceMoney=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP987654321098765\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7891\",\"price\":\"129.99\",\"seatClass\":2,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP987654321098765\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7891\",\"price\":\"129.99\",\"seatClass\":2,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("differenceMoney", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_60",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_60");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_60",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_60");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_60");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_60");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_61() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_61");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "125.75", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "249.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "E7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_61");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatNumber=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatNumber=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatNumber=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"249.50\",\"seatClass\":3,\"seatNumber\":\"\",\"status\":4,\"to\":\"Beijing\",\"trainNumber\":\"E7890\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"249.50\",\"seatClass\":3,\"seatNumber\":\"\",\"status\":4,\"to\":\"Beijing\",\"trainNumber\":\"E7890\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatNumber", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_61",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_61");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_61",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_61");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_61");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_61");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_62() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [documentType, contactsDocumentNumber, price, seatClass]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"7d6521ae-0ba8-423f-b2d9-a0dd05cf0bf6\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"\",\"contactsName\":...\nEnhanced Parameters: {documentType=2, contactsDocumentNumber=GB1234567890123456789, price=89.01, seatClass=5}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_62");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "GB12345678901234567890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Maria Garcia", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "89.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "7C", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_62");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "accountId=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "accountId=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("accountId=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"hangzhou\",\"id\":\"AR-7890\",\"price\":\"89.01\",\"seatClass\":4,\"seatNumber\":\"7C\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"hangzhou\",\"id\":\"AR-7890\",\"price\":\"89.01\",\"seatClass\":4,\"seatNumber\":\"7C\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("accountId", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_62",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_62");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_62",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_62");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_62");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_62");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_63() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"6b276e4d-75ba-49d2-b3e1-6c4f49cbfd4e\",\"boughtDate\":\"2026-02-14\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {price=899.00, from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=05B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_63");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-14", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "399.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "22:45:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_63");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "coachNumber=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "coachNumber=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("coachNumber=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":\"\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"899.00\",\"seatClass\":5,\"seatNumber\":\"05B\",\"status\":5,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":\"\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"899.00\",\"seatClass\":5,\"seatNumber\":\"05B\",\"status\":5,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("coachNumber", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_63",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_63");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_63",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_63");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_63");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_63");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_64() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, trainNumber, to]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"55f3660e-1875-437b-a8b0-ff9c85e24e8c\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsName\":\"Joh...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD9876543, from=beijing, trainNumber=D5678, to=guangzhou}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_64");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "tianjin", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ROUTE-456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "59.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_64");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelDate=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelDate=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelDate=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"ROUTE-456\",\"price\":\"59.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"ROUTE-456\",\"price\":\"59.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelDate", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_64",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_64");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_64",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_64");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_64");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_64");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_65() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"c435f19d-2c43-4114-a8fd-f181104fd8c2\",\"boughtDate\":\"\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsName\":\"Rob...\nEnhanced Parameters: {price=150.00, from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=25B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_65");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Robert Johnson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "SERVICE-789012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "2.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_65");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "boughtDate=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "boughtDate=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("boughtDate=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"SERVICE-789012\",\"price\":\"150.00\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"SERVICE-789012\",\"price\":\"150.00\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("boughtDate", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_65",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_65");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_65",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_65");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_65");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_65");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_66() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"6daaa0c3-72bf-4b5e-a9b9-c0622524e0c1\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=12A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_66");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "8", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_66");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "price=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "price=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("price=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("price", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_66",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_66");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_66",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_66");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_66");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_66");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_67() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"4d02be80-08c6-4501-93af-7c0721c849de\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD9876543, from=Shanghai, to=Guangzhou, trainNumber=D5678, seatNumber=25B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_67");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Emily Davis", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_67");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatClass=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatClass=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatClass=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":\"\",\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":\"\",\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatClass", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_67",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_67");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_67",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_67");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_67");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_67");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_68() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price, id, trainNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"08ad3ea6-3b62-4614-b1dc-88c3c3550d58\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD7654321, price=0.01, id=TS-67890, trainNumber=D5678}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_68");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-10.25", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "TS-12345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_68");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "from=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "from=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("from=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"\",\"id\":\"TS-67890\",\"price\":\"0.01\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":1,\"from\":\"\",\"id\":\"TS-67890\",\"price\":\"0.01\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("from", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_68",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_68");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_68",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_68");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_68");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_68");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_69() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"f04b6928-f8a8-4a2e-898c-d37d0101bf7d\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC005\",\"contacts...\nEnhanced Parameters: {accountId=ACC001, contactsDocumentNumber=CN12345678901234567, price=0.01}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_69");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC005", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "CN123456789012345678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Michael Wilson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-20", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_69");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "id=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "id=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("id=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN12345678901234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"\",\"price\":\"0.01\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN12345678901234567\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"\",\"price\":\"0.01\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("id", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_69",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_69");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_69",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_69");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_69");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_69");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_70() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, price, contactsDocumentNumber, to]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"c303c04f-b2b4-416f-adf6-995f64821e70\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, price=400.00, contactsDocumentNumber=CD9876543, to=Shanghai}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_70");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "15", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "4.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "18D", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_70");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters (ENHANCED)", "to=Shanghai");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "to=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("to=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"400.00\",\"seatClass\":1,\"seatNumber\":\"18D\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"CD9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"400.00\",\"seatClass\":1,\"seatNumber\":\"18D\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("to", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_70",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_70");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_70",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_70");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_70");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_70");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_71() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, travelDate, contactsDocumentNumber, price, boughtDate, from, to]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"6b7371e9-800f-49aa-974a-6f6b15995259\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsName\":\"John Sm...\nEnhanced Parameters: {accountId=ACC002, travelDate=2025-12-15, contactsDocumentNumber=US987654321, price=0.00, boughtDate=2025-12-10, from=Shanghai, to=Guangzhou}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_71");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "US123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "16.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_71");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "trainNumber=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "trainNumber=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("trainNumber=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2025-12-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US987654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":2,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"\",\"travelDate\":\"2025-12-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2025-12-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"US987654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"0.00\",\"seatClass\":2,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Guangzhou\",\"trainNumber\":\"\",\"travelDate\":\"2025-12-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("trainNumber", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_71",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_71");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_71",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_71");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_71");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_71");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_72() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, trainNumber, to]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"db28ec2d-0080-4355-a840-debe7f7eb3f2\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=XY9876543, from=shanghai, trainNumber=D5678, to=tianjin}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_72");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-25.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_72");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "status=");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "status=\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("status=");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"XY9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":\"\",\"to\":\"tianjin\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"XY9876543\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"shanghai\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":\"\",\"to\":\"tianjin\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("status", "");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_72",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_72");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_72",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_72");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_72");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_72");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_73() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_73");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "128.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_73");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"128.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"128.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_73",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_73");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_73",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_73");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_73");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_73");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_74() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_74");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "D5678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_74");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":2,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":3,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":2,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_74",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_74");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_74",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_74");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_74");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_74");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_75() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_75");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "JP123456789012345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "chengdu", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "25B", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_75");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"chengdu\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"chengdu\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_75",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_75");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_75",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_75");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_75");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_75");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_76() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_76");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ADMIN-001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_76");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Beijing\",\"id\":\"ADMIN-001\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_76",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_76");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_76",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_76");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_76");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_76");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_77() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_77");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "32.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "7C", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "C9012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_77");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"32.0\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"C9012\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"32.0\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"C9012\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_77",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_77");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_77",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_77");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_77");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_77");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_78() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_78");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "9j7f91h2-26hg-9hk6-g0gg-g1395bi", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "0.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "18D", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "T3456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "22:45:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_78");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"9j7f91h2-26hg-9hk6-g0gg-g1395bi\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"18D\",\"status\":2,\"to\":\"Beijing\",\"trainNumber\":\"T3456\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"9j7f91h2-26hg-9hk6-g0gg-g1395bi\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"18D\",\"status\":2,\"to\":\"Beijing\",\"trainNumber\":\"T3456\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_78",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_78");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_78",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_78");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_78");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_78");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_79() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_79");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "7h5d79f0-04fe-7fi4-e8ee-e91739g2gd9i", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Maria Garcia", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "125.75", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "ROUTE-456", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_79");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"7h5d79f0-04fe-7fi4-e8ee-e91739g2gd9i\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"7h5d79f0-04fe-7fi4-e8ee-e91739g2gd9i\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"125.75\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"ROUTE-456\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_79",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_79");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_79",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_79");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_79");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_79");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_80() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_80");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "GB12345678901234567890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "64.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_80");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB12345678901234567890\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"64.0\",\"seatClass\":5,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB12345678901234567890\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"64.0\",\"seatClass\":5,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-28\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_80",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_80");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_80",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_80");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_80");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_80");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_81() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_81");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-10.25", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_81");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":4,\"from\":\"shenzhen\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"-10.25\",\"documentType\":4,\"from\":\"shenzhen\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_81",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_81");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_81",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_81");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_81");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_81");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_82() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_82");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-14", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "CN123456789012345678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "25B", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "E7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_82");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN123456789012345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Hangzhou\",\"trainNumber\":\"E7890\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CN123456789012345678\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"25B\",\"status\":1,\"to\":\"Hangzhou\",\"trainNumber\":\"E7890\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_82",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_82");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_82",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_82");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_82");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_82");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_83() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_83");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_83");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":3,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_83",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_83");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_83",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_83");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_83");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_83");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_84() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_84");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_84");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_84",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_84");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_84",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_84");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_84");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_84");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_85() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_85");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "US123456789", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Robert Johnson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "nanjing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "7C", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_85");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"nanjing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":4,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"US123456789\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"nanjing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"7C\",\"status\":4,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_85",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_85");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_85",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_85");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_85");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_85");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_86() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_86");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "8", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_86");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":2,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":2,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_86",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_86");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_86",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_86");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_86");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_86");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_87() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_87");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "5f3b57d8-82dc-5dg2-c6cc-c79517e0eb7g", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-28", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "JP123456789012345", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Emily Davis", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "-25.50", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "512.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "2", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "18D", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "D5678", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_87");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"5f3b57d8-82dc-5dg2-c6cc-c79517e0eb7g\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"512.0\",\"seatClass\":2,\"seatNumber\":\"18D\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"5f3b57d8-82dc-5dg2-c6cc-c79517e0eb7g\",\"boughtDate\":\"2026-02-28\",\"coachNumber\":12,\"contactsDocumentNumber\":\"JP123456789012345\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"-25.50\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"512.0\",\"seatClass\":2,\"seatNumber\":\"18D\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"D5678\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_87",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_87");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_87",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_87");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_87");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_87");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_88() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_88");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "6g4c68e9-93ed-6eh3-d7dd-d80628f1fc8h", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "SERVICE-789012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_88");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"6g4c68e9-93ed-6eh3-d7dd-d80628f1fc8h\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"SERVICE-789012\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"6g4c68e9-93ed-6eh3-d7dd-d80628f1fc8h\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"SERVICE-789012\",\"price\":\"129.99\",\"seatClass\":3,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_88",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_88");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_88",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_88");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_88");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_88");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_89() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_89");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "15", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "1024.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-02-20", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_89");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"1024.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":5,\"to\":\"Guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":3,\"from\":\"Shanghai\",\"id\":\"AR-7890\",\"price\":\"1024.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":5,\"to\":\"Guangzhou\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-02-20\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_89",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_89");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_89",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_89");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_89");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_89");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_90() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [contactsDocumentNumber, price, id]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"39a4bcd5-450d-40d3-a2e8-202762424b57\",\"boughtDate\":\"2026-03-10\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"' OR 1=1--\",\"accountId\":\"8i6e80g1-15gf-8...\nEnhanced Parameters: {contactsDocumentNumber=GB1234567890123456789, price=129.99, id=AR-789}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_90");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "8i6e80g1-15gf-8gj5-f9ff-f0284ah3he0j", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "GB12345678901234567890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "0.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Guangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "' OR 1=1--", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_90");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelTime=' OR 1=1--");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelTime=' OR 1=1--\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelTime=' OR 1=1--");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"8i6e80g1-15gf-8gj5-f9ff-f0284ah3he0j\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":1,\"from\":\"Guangzhou\",\"id\":\"AR-789\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"' OR 1=1--\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"8i6e80g1-15gf-8gj5-f9ff-f0284ah3he0j\",\"boughtDate\":\"2026-03-10\",\"coachNumber\":12,\"contactsDocumentNumber\":\"GB1234567890123456789\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"0.00\",\"documentType\":1,\"from\":\"Guangzhou\",\"id\":\"AR-789\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"' OR 1=1--\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelTime", "' OR 1=1--");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_90",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_90");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_90",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_90");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_90");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_90");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_POST_101_91() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_POST_101_91");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-03-10", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_POST_101_91");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-08\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shenzhen\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-03-10\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // ✅ POSITIVE TEST VALIDATION
                        // Positive test: PASS only if status == expected AND response body indicates success
                        if (actualStatusCode1 != expectedStatusCode1) {
                            throw new AssertionError("Expected status code " + expectedStatusCode1 + ", but got: " + actualStatusCode1);
                        }
                        
                        // LLM validation for positive tests - detect soft errors in 2XX responses
                        if (llmValidationEnabled && (!only2xx || (actualStatusCode1 >= 200 && actualStatusCode1 < 300))) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder"
                                    );
                                
                                System.out.println("🤖 LLM Validation (Positive Test): Failed=" + validationResult.isFailed() + ", RCA: " + validationResult.getRca());
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Positive Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: ✅ POSITIVE\n");
                                    llmReport.append("Analysis Result: ").append(validationResult.isFailed() ? "❌ SOFT ERROR DETECTED" : "✅ VALID SUCCESS").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(validationResult.getRca()).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Positive Test)", "text/plain", llmReport.toString());
                                }
                                
                                // Positive test logic:
                                // - If LLM says FAILED: FAIL (we expected success but got soft error)
                                // - If LLM says SUCCESS: PASS (as expected)
                                if (validationResult.isFailed()) {
                                    throw new AssertionError("Positive test failed: Expected success but LLM detected soft error. RCA: " + validationResult.getRca());
                                }
                                System.out.println("✅ Positive test PASSED: LLM validated response as successful");
                                
                            } catch (AssertionError ae) {
                                throw ae; // Re-throw assertion errors
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Don't fail the test due to LLM validation errors
                            }
                        }
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_POST_101_91",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_POST_101_91");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_POST_101_91",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_POST_101_91");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_POST_101_91");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_POST_101_91");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_92() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_92");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "' OR 1=1--", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Shenzhen", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "256.0", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_92");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "documentType=' OR 1=1--");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "documentType=' OR 1=1--\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("documentType=' OR 1=1--");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"' OR 1=1--\",\"from\":\"Shenzhen\",\"id\":\"AR-7890\",\"price\":\"256.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":\"' OR 1=1--\",\"from\":\"Shenzhen\",\"id\":\"AR-7890\",\"price\":\"256.0\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("documentType", "' OR 1=1--");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_92",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_92");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_92",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_92");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_92");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_92");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_93() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, documentType, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"58984720-26d5-4642-9346-8162601f3483\",\"boughtDate\":\"2026-02-14\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, documentType=2, seatClass=2, status=2}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_93");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-02-14", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "3", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "' OR '1'='1--", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Michael Wilson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "22:45:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_93");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsDocumentNumber=' OR '1'='1--");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsDocumentNumber=' OR '1'='1--\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsDocumentNumber=' OR '1'='1--");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":3,\"contactsDocumentNumber\":\"' OR '1'='1--\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-02-14\",\"coachNumber\":3,\"contactsDocumentNumber\":\"' OR '1'='1--\",\"contactsName\":\"Michael Wilson\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"22:45:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsDocumentNumber", "' OR '1'='1--");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_93",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_93");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_93",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_93");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_93");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_93");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_94() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, coachNumber, documentType, price, seatClass, status]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"69deea6e-5c9b-4965-8a80-b4b0c90ef19e\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\",\"accountId\":\"ACC001\",\"contacts...\nEnhanced Parameters: {accountId=ACC002, coachNumber=1, documentType=1, price=0.01, seatClass=3, status=0}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_94");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "' OR '1'='1", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "3F", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "06:00:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_94");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "contactsName=' OR '1'='1");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "contactsName=' OR '1'='1\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("contactsName=' OR '1'='1");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"' OR '1'='1\",\"differenceMoney\":\"50.00\",\"documentType\":4,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"0.01\",\"seatClass\":1,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"' OR '1'='1\",\"differenceMoney\":\"50.00\",\"documentType\":4,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"0.01\",\"seatClass\":1,\"seatNumber\":\"3F\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"06:00:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("contactsName", "' OR '1'='1");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_94",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_94");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_94",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_94");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_94");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_94");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_95() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [coachNumber, price, from, to, trainNumber, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"eb41e83c-b258-46b4-aea1-a4c27d9aec40\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"4d2a46c7-71cb-4cf...\nEnhanced Parameters: {coachNumber=12, price=129.99, from=Beijing, to=Shanghai, trainNumber=G1234, seatNumber=12A}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_95");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "' OR '1'='1--", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_95");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "differenceMoney=' OR '1'='1--");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "differenceMoney=' OR '1'='1--\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("differenceMoney=' OR '1'='1--");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"' OR '1'='1--\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":12,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"' OR '1'='1--\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("differenceMoney", "' OR '1'='1--");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_95",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_95");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_95",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_95");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_95");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_95");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_96() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_96");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Maria Garcia", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "' OR '1'='1", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "18:20:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_96");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "seatNumber=' OR '1'='1");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "seatNumber=' OR '1'='1\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("seatNumber=' OR '1'='1");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"' OR '1'='1\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":4,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Maria Garcia\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"' OR '1'='1\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"18:20:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("seatNumber", "' OR '1'='1");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_96",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_96");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_96",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_96");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_96");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_96");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_97() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [coachNumber, travelDate, documentType, boughtDate, price, seatClass]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"5807580c-15aa-4a51-a6ac-c68eec1237a0\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"08:30:00\",\"accountId\":\"' OR '1'='1\",\"con...\nEnhanced Parameters: {coachNumber=1, travelDate=2023-01-15, documentType=1, boughtDate=2023-01-15, price=129.99, seatClass=1}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_97");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "' OR '1'='1", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "8", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Robert Johnson", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "4", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_97");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "accountId=' OR '1'='1");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "accountId=' OR '1'='1\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("accountId=' OR '1'='1");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"' OR '1'='1\",\"boughtDate\":\"2023-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2023-01-15\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"' OR '1'='1\",\"boughtDate\":\"2023-01-15\",\"coachNumber\":8,\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"Robert Johnson\",\"differenceMoney\":\"50.00\",\"documentType\":5,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":4,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2023-01-15\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("accountId", "' OR '1'='1");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_97",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_97");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_97",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_97");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_97");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_97");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_98() throws Exception {
        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_98");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "' OR 1=1--", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "5", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "14:15:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_98");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "coachNumber=' OR 1=1--");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "coachNumber=' OR 1=1--\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("coachNumber=' OR 1=1--");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":\"' OR 1=1--\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":5,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC001\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":\"' OR 1=1--\",\"contactsDocumentNumber\":\"AB1234567\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":5,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Beijing\",\"trainNumber\":\"G1234\",\"travelDate\":\"2026-01-15\",\"travelTime\":\"14:15:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("coachNumber", "' OR 1=1--");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_98",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_98");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_98",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_98");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_98");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_98");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_99() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, price, from, trainNumber, to]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Success\",\"data\":{\"id\":\"caf4505f-8611-4109-94e4-55b5a86e93a8\",\"boughtDate\":\"2026-01-15\",\"travelDate\":\"' OR '1'='1 --\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"contactsName\":\"Joh...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD7654321, price=299.99, from=Beijing, trainNumber=D8765, to=Shanghai}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_99");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "2026-01-15", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "15", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "John Smith", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "Hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Hangzhou", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "C9012", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "' OR '1'='1 --", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_99");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "travelDate=' OR '1'='1 --");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "travelDate=' OR '1'='1 --\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("travelDate=' OR '1'='1 --");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"299.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"D8765\",\"travelDate\":\"' OR '1'='1 --\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"2026-01-15\",\"coachNumber\":15,\"contactsDocumentNumber\":\"CD7654321\",\"contactsName\":\"John Smith\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"Beijing\",\"id\":\"AR-7890\",\"price\":\"299.99\",\"seatClass\":1,\"seatNumber\":\"12A\",\"status\":1,\"to\":\"Shanghai\",\"trainNumber\":\"D8765\",\"travelDate\":\"' OR '1'='1 --\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("travelDate", "' OR '1'='1 --");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_99",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_99");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_99",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_99");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_99");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_99");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

    @Test
    public void test_negative_POST_101_100() throws Exception {
        // ═══════════════════════════════════════════════════════════════════════
        // 🔧 ENHANCED TEST - Modified by Test Case Enhancer
        // Original Status: 200
        // Enhanced Parameters: [accountId, contactsDocumentNumber, from, trainNumber, to, seatNumber]
        // ═══════════════════════════════════════════════════════════════════════
        Allure.label("enhancement", "ENHANCED");
        Allure.addAttachment("📝 Original Failure", "text/plain", "Original Status: 200\nOriginal Response: {\"status\":1,\"msg\":\"Add new Order Success\",\"data\":{\"id\":\"8711b611-ccda-480b-9cfe-d5be69a41b67\",\"boughtDate\":\"' OR '1'='1 --\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\",\"accountId\":\"ACC001\",\"cont...\nEnhanced Parameters: {accountId=ACC002, contactsDocumentNumber=CD2345678, from=beijing, trainNumber=G5678, to=shanghai, seatNumber=15B}");

        // Record test execution for fault detection tracking
        es.us.isa.restest.analysis.FaultDetectionTracker.getInstance()
            .recordTestCase(this.getClass().getName(), "test_negative_POST_101_100");

        // 🔧 Test Case Enhancer: Capture test metadata
        es.us.isa.restest.enhancer.TestResultCapture.setTestMetadata(
            "/api/v1/adminorderservice/adminorder", "POST", 
            "ts-admin-order-service", true);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "accountId", "ACC001", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "boughtDate", "' OR '1'='1 --", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "coachNumber", "12", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsDocumentNumber", "AB1234567", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "contactsName", "Emily Davis", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "differenceMoney", "50.00", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "documentType", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "from", "shanghai", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "id", "AR-7890", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "price", "129.99", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatClass", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "seatNumber", "12A", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "status", "1", 
            "integer", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "to", "Beijing", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "trainNumber", "G1234", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelDate", "2026-01-08", 
            "string", "formData", 
            null, 
            null, false);
        es.us.isa.restest.enhancer.TestResultCapture.addParameter(
            "travelTime", "08:30:00", 
            "string", "formData", 
            null, 
            null, false);

        // 🎯 Allure Suite Grouping: Normalize by API template (not by resolved path values)
        Allure.label("parentSuite", "ts-admin-order-service");
        Allure.label("suite", "POST /api/v1/adminorderservice/adminorder");
        Allure.label("subSuite", "test_negative_POST_101_100");

        // 🚨 NEGATIVE TEST METADATA
        Allure.parameter("🚨 Test Type", "NEGATIVE (Invalid Input Testing)");
        Allure.parameter("🔴 Invalid Parameters", "boughtDate=' OR '1'='1 --");
        Allure.description("**⚠️ This is a NEGATIVE test case with intentionally invalid inputs.**\n\n" +
                         "The following parameters were intentionally set to invalid values:\n" +
                         "boughtDate=' OR '1'='1 --\n\n" +
                         "This test expects a 4XX or 5XX error response. If it returns 2XX, the test will FAIL.");
        es.us.isa.restest.enhancer.TestResultCapture.addInvalidParameter("boughtDate=' OR '1'='1 --");

        final String[] _jwt     = new String[1];
        final String[] _jwtType = new String[1];
        final java.util.concurrent.atomic.AtomicBoolean loginSucceeded  = new java.util.concurrent.atomic.AtomicBoolean(true);
        final java.util.concurrent.atomic.AtomicBoolean scenarioFailed = new java.util.concurrent.atomic.AtomicBoolean(false);
        // 🔐 STEP 0: Authentication - Clean reporting
        Allure.step("🔐 Step 0: Authentication (Login)", () -> {
            try {
                Allure.parameter("🏢 Service", "Authentication Service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/users/login");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("👤 Username", "admin");
                Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - Authentication required");
                Allure.description("🔐 **Authentication Step**\n" +
                                 "This step authenticates the user to obtain JWT token for subsequent API calls.\n" +
                                 "All other steps depend on successful authentication.");
                
                Response loginRes = RestAssured.given()
                    .contentType("application/json")
                    .body("{\"username\":\"admin\",\"password\":\"222222\"}")
                .when().post("/api/v1/users/login")
                    .then().log().ifValidationFails()
                    .statusCode(200)  // Login expects 200 - could be configurable in future
                    .extract().response();
                _jwt[0]     = loginRes.jsonPath().getString("data.token");
                _jwtType[0] = "Bearer";
                
                // ✅ SUCCESS: Clean login success reporting
                String tokenObtained = _jwt[0] != null ? "Yes" : "No";
                Allure.parameter("🎯 Result", "✅ SUCCESS (Token: " + tokenObtained + ")");
                Allure.addAttachment("🔐 Login Response", "application/json", loginRes.getBody().asString());
            } catch (Throwable loginError) {
                loginSucceeded.set(false);
                
                // ❌ FAILURE: Clean login failure reporting
                String errorType = loginError.getClass().getSimpleName();
                if (loginError instanceof java.net.ConnectException) {
                    errorType = "Connection Failed";
                } else if (loginError instanceof AssertionError) {
                    errorType = "Authentication Failed";
                }
                
                Allure.parameter("🎯 Result", "❌ FAILED (" + errorType + ")");
                Allure.addAttachment("💥 Login Error", "text/plain", "Error: " + errorType + "\nMessage: " + loginError.getMessage());
                
                // Throw to mark login step as failed in Allure
                throw new RuntimeException("Login failed: " + loginError.getMessage(), loginError);
            }
        });
        String jwt     = _jwt[0];
        String jwtType = _jwtType[0];

        // Step execution results tracking
        final java.util.Map<Integer, Boolean> stepResults = new java.util.HashMap<>();
        final java.util.Map<Integer, String> capturedOutputs = new java.util.HashMap<>();
        // Parameter tracking for error analysis
        final java.util.Map<String, String> allStepParameters = new java.util.HashMap<>();

        // Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]
        // 🔥 ALWAYS create Allure step - execution decision happens INSIDE
        
        // 🎯 CRITICAL: Add delay between test executions to ensure unique traces
        // This prevents tests from executing so rapidly that they find the same traces
        try {
            Thread.sleep(2000); // 2 second delay between tests for trace separation
        } catch (InterruptedException ie) {
            Thread.currentThread().interrupt();
        }
        
        final long requestStartMicros = System.currentTimeMillis() * 1000L;
        try {
            Allure.step("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]", () -> {
                Allure.parameter("🏢 Service", "ts-admin-order-service");
                Allure.parameter("📡 HTTP Method", "POST");
                Allure.parameter("🔗 Endpoint", "/api/v1/adminorderservice/adminorder");
                Allure.parameter("🎯 Expected Status", 200);
                Allure.parameter("🔗 Dependency Type", "INDEPENDENT (can execute regardless of other step results)");
                Allure.description("🎯 **Testing**: ts-admin-order-service\n" +
                                 "📡 **Method**: POST\n" +
                                 "🔗 **Path**: /api/v1/adminorderservice/adminorder\n" +
                                 "🎯 **Expected**: 200\n" +
                                 "🔗 **Dependencies**: INDEPENDENT (can execute regardless of other step results)");
                
                // Execution decision analysis - determine if step should execute
                boolean shouldSkip = false;
                String skipReason = "";
                String skipCategory = "";
                
                // Check authentication dependency
                if (!loginSucceeded.get()) {
                    shouldSkip = true;
                    skipReason = "Authentication failed - cannot proceed with authenticated API calls";
                    skipCategory = "🔐 AUTH_FAILED";
                }
                
                // Add execution decision as parameter
                if (shouldSkip) {
                    Allure.parameter("🚦 Execution Decision", "⏭️ SKIP - " + skipCategory);
                    Allure.parameter("⏭️ Skip Reason", skipReason);
                } else {
                    Allure.parameter("🚦 Execution Decision", "▶️ EXECUTE - All dependencies satisfied");
                }
                
                if (!shouldSkip) {
                    System.out.println("▶️ EXECUTING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] (dependency analysis passed)");
                    Response stepResponse1 = null;
                    
                    try {
                        RequestSpecification req = RestAssured.given();
                        // 🔥 FIX: Set Content-Type to application/json for requests with bodies
                        req = req.contentType("application/json");
                        String requestBody1 = "{\"accountId\":\"ACC002\",\"boughtDate\":\"' OR '1'='1 --\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD2345678\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"15B\",\"status\":1,\"to\":\"shanghai\",\"trainNumber\":\"G5678\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}";
                        req = req.body(requestBody1);
                        
                        // Add request body as attachment (AllureRestAssured filter disabled to avoid duplication)
                        Allure.addAttachment("📤 Request Body", "application/json", requestBody1);
                        if (loginSucceeded.get()) {
                            req = req.header("Authorization", jwtType + " " + jwt);
                        }
                        allStepParameters.put("body", "{\"accountId\":\"ACC002\",\"boughtDate\":\"' OR '1'='1 --\",\"coachNumber\":12,\"contactsDocumentNumber\":\"CD2345678\",\"contactsName\":\"Emily Davis\",\"differenceMoney\":\"50.00\",\"documentType\":1,\"from\":\"beijing\",\"id\":\"AR-7890\",\"price\":\"129.99\",\"seatClass\":1,\"seatNumber\":\"15B\",\"status\":1,\"to\":\"shanghai\",\"trainNumber\":\"G5678\",\"travelDate\":\"2026-01-08\",\"travelTime\":\"08:30:00\"}");
                        // 🔥 FIX: Extract response FIRST (before status code assertion) to capture response body in all cases
                        stepResponse1 = req.when().post("/api/v1/adminorderservice/adminorder")
                               .then().log().ifValidationFails()
                               .extract().response();
                        
                        // Now validate status code based on expected status from configuration
                        int actualStatusCode1 = stepResponse1.getStatusCode();
                        int expectedStatusCode1 = 200;
                        
                        // 🤖 LLM RESPONSE VALIDATION: Check response body for errors
                        boolean llmValidationEnabled = Boolean.parseBoolean(System.getProperty("llm.response.validation.enabled", "false"));
                        boolean only2xx = Boolean.parseBoolean(System.getProperty("llm.response.validation.only.2xx", "true"));
                        boolean includeRca = Boolean.parseBoolean(System.getProperty("llm.response.validation.include.rca", "true"));
                        
                        // 🔴 NEGATIVE TEST VALIDATION
                        // For negative tests with invalid inputs, we expect EITHER:
                        // 1. A different status code (error response), OR
                        // 2. Status 200 but with error/failure message in response body (soft error)
                        // CRITICAL: The error message MUST be related to our designed invalid input!
                        boolean statusCodeIndicatesError = (actualStatusCode1 != expectedStatusCode1);
                        boolean llmDetectedRelatedError = false;
                        String llmRca = "LLM validation not performed";
                        
                        // Build map of designed invalid parameters for this negative test
                        java.util.Map<String, String> invalidParams = new java.util.HashMap<>();
                        invalidParams.put("boughtDate", "' OR '1'='1 --");
                        
                        // Always run LLM validation for negative tests (to detect soft errors related to our invalid input)
                        if (llmValidationEnabled) {
                            try {
                                String validationBody = stepResponse1.getBody().asString();
                                
                                // Create LLM validator instance
                                es.us.isa.restest.generators.ZeroShotLLMGenerator llmValidator = new es.us.isa.restest.generators.ZeroShotLLMGenerator();
                                
                                // Validate response - checks if error is RELATED to our invalid input
                                es.us.isa.restest.generators.ZeroShotLLMGenerator.ValidationResult validationResult = 
                                    llmValidator.validateNegativeTestResponse(
                                        actualStatusCode1,
                                        validationBody,
                                        "ts-admin-order-service",
                                        "POST",
                                        "/api/v1/adminorderservice/adminorder",
                                        invalidParams
                                    );
                                
                                // isFailed() returns true only if error was detected AND related to our invalid input
                                llmDetectedRelatedError = validationResult.isFailed();
                                llmRca = validationResult.getRca();
                                System.out.println("🤖 LLM Validation (Negative Test): Error Related to Invalid Input=" + llmDetectedRelatedError + ", RCA: " + llmRca);
                                
                                // Attach LLM analysis to Allure report
                                if (includeRca) {
                                    StringBuilder llmReport = new StringBuilder();
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n");
                                    llmReport.append("🤖 INTELLIGENT ANALYSIS (Negative Test)\n");
                                    llmReport.append("════════════════════════════════════════════════════════════════════════\n\n");
                                    llmReport.append("Test Type: 🔴 NEGATIVE (Invalid Input Testing)\n\n");
                                    
                                    // Show designed invalid inputs
                                    llmReport.append("📋 DESIGNED INVALID INPUTS:\n");
                                    for (java.util.Map.Entry<String, String> entry : invalidParams.entrySet()) {
                                        llmReport.append("   • ").append(entry.getKey()).append(" = ").append(entry.getValue()).append("\n");
                                    }
                                    llmReport.append("\n");
                                    
                                    llmReport.append("Status Code: ").append(actualStatusCode1).append(" (expected: ").append(expectedStatusCode1).append(")\n");
                                    llmReport.append("Status Code Indicates Error: ").append(statusCodeIndicatesError ? "✅ YES" : "❌ NO").append("\n");
                                    llmReport.append("Error Related to Invalid Input: ").append(llmDetectedRelatedError ? "✅ YES" : "❌ NO").append("\n\n");
                                    llmReport.append("Root Cause Analysis:\n");
                                    llmReport.append(llmRca).append("\n");
                                    Allure.addAttachment("🤖 INTELLIGENT ANALYSIS (Negative Test)", "text/plain", llmReport.toString());
                                }
                                
                            } catch (Exception llmEx) {
                                System.err.println("⚠️ LLM validation failed: " + llmEx.getMessage());
                                // Continue with status code check only
                            }
                        }
                        
                        // Negative test PASSES if either:
                        // 1. Status code is different from expected (clear error), OR
                        // 2. LLM detected a soft error that is RELATED to our designed invalid input
                        // NOTE: If error is NOT related to our invalid input, test still FAILS!
                        boolean negativeTestPassed = statusCodeIndicatesError || llmDetectedRelatedError;
                        
                        if (!negativeTestPassed) {
                            throw new AssertionError("Negative test failed: Either no error detected, or error was not related to our invalid input. RCA: " + llmRca);
                        }
                        
                        if (statusCodeIndicatesError) {
                            System.out.println("✅ Negative test PASSED: Status code " + actualStatusCode1 + " indicates error (expected: " + expectedStatusCode1 + ")");
                        } else {
                            System.out.println("✅ Negative test PASSED: LLM confirmed error is related to our designed invalid input");
                        }
                        
                        
                        // 🔍 FAULT DETECTION: Check if root API response contains injected fault
                        try {
                            String faultCheckBody = stepResponse1.getBody().asString();
                            org.json.JSONObject faultJson = new org.json.JSONObject(faultCheckBody);
                            if (faultJson.has("data")) {
                                Object dataValue = faultJson.get("data");
                                if (dataValue instanceof org.json.JSONObject) {
                                    org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                    if (dataObj.optBoolean("injected", false)) {
                                        String detectedFaultName = dataObj.optString("faultName", "");
                                        if (!detectedFaultName.isEmpty()) {
                                            es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                detectedFaultName,
                                                this.getClass().getName(),
                                                "test_negative_POST_101_100",
                                                System.currentTimeMillis(),
                                                faultCheckBody
                                            );
                                            System.out.println("🔍 FAULT DETECTED: " + detectedFaultName + " in test: test_negative_POST_101_100");
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break test execution
                        }
                        
                        stepResults.put(1, true);
                        System.out.println("✅ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - SUCCESS");
                        // ✅ SUCCESS: Clean success reporting without duplication
                        try {
                            String responseBody = stepResponse1.getBody().asString();
                            int actualStatus = stepResponse1.getStatusCode();
                            long responseTime = stepResponse1.getTime();
                            
                            // 🔧 Test Case Enhancer: Capture response for enhancement
                            es.us.isa.restest.enhancer.TestResultCapture.captureResponse(actualStatus, responseBody);
                            
                            // Single success status parameter
                            Allure.parameter("🎯 Result", "✅ SUCCESS (" + actualStatus + " in " + responseTime + "ms)");
                            
                            // Add response as attachment (AllureRestAssured filter disabled to avoid duplication)
                            Allure.addAttachment("📥 Response (" + actualStatus + ")", "application/json", responseBody);
                            // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                            try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                            attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, false);
                        } catch (Exception e) {
                            Allure.parameter("🎯 Result", "✅ SUCCESS (response capture failed)");
                        }
                    } catch (Throwable t) {
                        stepResults.put(1, false);
                        System.out.println("❌ Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - FAILED: " + t.getMessage());
                        
                        // 🔥 CRITICAL: Capture response body for failed requests (response was extracted before assertion)
                        String failedResponseBody = null;
                        int failedStatusCode = -1;
                        long failedResponseTime = -1;
                        try {
                            if (stepResponse1 != null) {
                                failedResponseBody = stepResponse1.getBody().asString();
                                failedStatusCode = stepResponse1.getStatusCode();
                                failedResponseTime = stepResponse1.getTime();
                                // 🔧 Test Case Enhancer: Capture response for enhancement
                                es.us.isa.restest.enhancer.TestResultCapture.captureResponse(failedStatusCode, failedResponseBody);
                            }
                        } catch (Exception respEx) {
                            failedResponseBody = "Unable to capture response: " + respEx.getMessage();
                        }
                        
                        // 🔍 FAULT DETECTION (FAILURE PATH): Check if failed response contains injected fault
                        try {
                            if (failedResponseBody != null && !failedResponseBody.startsWith("Unable to capture")) {
                                org.json.JSONObject faultJson = new org.json.JSONObject(failedResponseBody);
                                if (faultJson.has("data")) {
                                    Object dataValue = faultJson.get("data");
                                    if (dataValue instanceof org.json.JSONObject) {
                                        org.json.JSONObject dataObj = (org.json.JSONObject) dataValue;
                                        if (dataObj.optBoolean("injected", false)) {
                                            String detectedFaultName = dataObj.optString("faultName", "");
                                            if (!detectedFaultName.isEmpty()) {
                                                es.us.isa.restest.analysis.FaultDetectionTracker.getInstance().recordDetectedFault(
                                                    detectedFaultName,
                                                    this.getClass().getName(),
                                                    "test_negative_POST_101_100",
                                                    System.currentTimeMillis(),
                                                    failedResponseBody
                                                );
                                                System.out.println("🔍 FAULT DETECTED (in failed test): " + detectedFaultName + " in test: test_negative_POST_101_100");
                                            }
                                        }
                                    }
                                }
                            }
                        } catch (Exception faultEx) {
                            // Silent fail - don't break error handling
                        }
                        
                        // ❌ FAILURE: Enhanced failure reporting with detailed analysis
                        String errorType = t.getClass().getSimpleName();
                        String failureReason = "";
                        if (t instanceof java.net.ConnectException) {
                            errorType = "Connection Failed";
                            failureReason = "Service unreachable - Connection refused";
                        } else if (t instanceof AssertionError) {
                            errorType = "Status Code Mismatch";
                            failureReason = "Expected vs actual status code mismatch";
                        } else if (t instanceof java.net.SocketTimeoutException) {
                            errorType = "Request Timeout";
                            failureReason = "Service response timeout";
                        } else if (t.getMessage() != null && t.getMessage().contains("404")) {
                            errorType = "Not Found (404)";
                            failureReason = "API endpoint not found";
                        } else if (t.getMessage() != null && t.getMessage().contains("500")) {
                            errorType = "Internal Server Error (500)";
                            failureReason = "Service internal error";
                        } else {
                            failureReason = "Unexpected error: " + errorType;
                        }
                        
                        // Enhanced failure parameters
                        String resultMsg = "❌ FAILED (" + errorType;
                        if (failedStatusCode >= 0) {
                            resultMsg += ", status: " + failedStatusCode;
                        }
                        if (failedResponseTime >= 0) {
                            resultMsg += ", " + failedResponseTime + "ms";
                        }
                        resultMsg += ")";
                        Allure.parameter("🎯 Result", resultMsg);
                        Allure.parameter("🔍 Failure Reason", failureReason);
                        Allure.parameter("🏢 Failed Service", "ts-admin-order-service");
                        Allure.parameter("📡 Failed Method", "POST");
                        Allure.parameter("🔗 Failed Endpoint", "/api/v1/adminorderservice/adminorder");
                        
                        // 🔥 CRITICAL: Attach response body for failed requests
                        if (failedResponseBody != null) {
                            String responseTitle = "📥 Response (" + failedStatusCode + ")";
                            Allure.addAttachment(responseTitle, "application/json", failedResponseBody);
                        }
                        
                        // Comprehensive error details
                        StringBuilder errorDetails = new StringBuilder();
                        errorDetails.append("❌ STEP FAILURE ANALYSIS\n");
                        errorDetails.append("=====================================\n\n");
                        errorDetails.append("📋 Step: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]\n");
                        errorDetails.append("🏢 Service: ts-admin-order-service\n");
                        errorDetails.append("📡 Method: POST\n");
                        errorDetails.append("🔗 Endpoint: /api/v1/adminorderservice/adminorder\n");
                        errorDetails.append("💥 Error Type: ").append(errorType).append("\n");
                        errorDetails.append("🔍 Reason: ").append(failureReason).append("\n\n");
                        errorDetails.append("📜 Full Error Message:\n");
                        errorDetails.append(t.getMessage() != null ? t.getMessage() : "No message").append("\n\n");
                        if (t.getCause() != null) {
                            errorDetails.append("🔗 Root Cause:\n");
                            errorDetails.append(t.getCause().toString()).append("\n\n");
                        }
                        errorDetails.append("🔧 Troubleshooting Tips:\n");
                        if (errorType.contains("Connection Failed")) {
                            errorDetails.append("• Check if the service is running\n• Verify network connectivity\n• Check firewall settings\n");
                        } else if (errorType.contains("Timeout")) {
                            errorDetails.append("• Service may be overloaded\n• Increase timeout settings\n• Check service health\n");
                        } else if (errorType.contains("404")) {
                            errorDetails.append("• Verify API endpoint exists\n• Check service deployment\n• Review API documentation\n");
                        } else if (errorType.contains("500")) {
                            errorDetails.append("• Check service logs\n• Verify service configuration\n• Review dependencies\n");
                        } else {
                            errorDetails.append("• Review full error message\n• Check service status\n• Verify request parameters\n");
                        }
                        
                        // ⏱️ Wait longer for trace propagation to Jaeger (increased delay)
                        try { Thread.sleep(3000); } catch (InterruptedException ie) { Thread.currentThread().interrupt(); }
                        attachJaegerTrace("ts-admin-order-service", "POST", "/api/v1/adminorderservice/adminorder", requestStartMicros, allStepParameters, true);
                        
                        // 🔥 CRITICAL: Throw exception to mark step as FAILED (red arrow) in Allure
                        throw new RuntimeException("Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] failed: " + failureReason + " (" + errorType + ")", t);
                    }
                } else {
                    // ⏭️ SKIP: Clean skip reporting with proper Allure status
                    System.out.println("⏭️ SKIPPING: Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200] - " + skipReason);
                    stepResults.put(1, false);
                    
                    // Single skip status parameter
                    Allure.parameter("🎯 Result", "⏭️ SKIPPED (" + skipCategory.replaceAll("🔐 |📊 |🔄 ", "") + ")");
                    
                    // Single skip reason attachment
                    Allure.addAttachment("⏭️ Skip Details", "text/plain", "Reason: " + skipReason);
                    
                    // 🔥 CRITICAL: Throw Assumption exception to mark step as SKIPPED (yellow arrow) in Allure
                    throw new org.junit.AssumptionViolatedException("Step skipped: " + skipReason);
                }
            });
        } catch (Exception stepException) {
            // Step wrapper exception handling - maintain execution flow
            if (stepException instanceof RuntimeException && stepException.getMessage().startsWith("Step failed:")) {
                // This is a failed step - already handled, just continue
                System.out.println("Step 1 marked as FAILED in Allure");
            } else if (stepException instanceof org.junit.AssumptionViolatedException) {
                // This is a skipped step - already handled, just continue
                System.out.println("Step 1 marked as SKIPPED in Allure");
            } else {
                // Unexpected wrapper failure
                System.out.println("⚠️ Step wrapper failed for Step 1: ts-admin-order-service POST /api/v1/adminorderservice/adminorder [expect not 200]: " + stepException.getMessage());
                stepResults.put(1, false);
            }
        }

        // Evaluate scenario result with comprehensive reporting
        long successfulSteps = stepResults.values().stream().filter(result -> result).count();
        long failedSteps = stepResults.values().stream().filter(result -> !result).count();
        long totalSteps = stepResults.size();
        
        // Add clean test summary - no duplicate content
        String overallResult;
        String severity;
        if (!loginSucceeded.get()) {
            overallResult = "❌ AUTHENTICATION FAILED";
            severity = "critical";
        } else if (failedSteps == 0) {
            overallResult = "✅ ALL STEPS PASSED";
            severity = "normal";
        } else if (successfulSteps > 0) {
            overallResult = "⚠️ PARTIAL FAILURE";
            severity = "major";
        } else {
            overallResult = "❌ ALL STEPS FAILED";
            severity = "critical";
        }
        
        // Single summary parameter with all key info
        Allure.parameter("📊 Scenario Result", overallResult + " (" + successfulSteps + "/" + totalSteps + " steps)");
        
        // Add clean categorization
        Allure.label("severity", severity);
        Allure.label("feature", "Microservice Workflow");
        Allure.label("story", "test_negative_POST_101_100");
        Allure.description("Microservice test scenario with " + totalSteps + " steps.");
        
        System.out.println("=== SCENARIO RESULT ===");
        System.out.println("Scenario: test_negative_POST_101_100");
        System.out.println("Total Steps: " + totalSteps);
        System.out.println("Successful: " + successfulSteps);
        System.out.println("Failed: " + failedSteps);
        System.out.println("Login Status: " + (loginSucceeded.get() ? "SUCCESS" : "FAILED"));
        
        // IMPROVED: Test fails if ANY step fails or login fails (not just when ALL fail)
        if (!loginSucceeded.get()) {
            fail("Scenario FAILED: Authentication failed - cannot proceed with API calls");
        } else if (failedSteps > 0) {
            fail("Scenario FAILED: " + failedSteps + " out of " + totalSteps + " steps failed. " +
                 "In microservice testing, all workflow steps must succeed for end-to-end validation.");
        } else if (successfulSteps == 0) {
            fail("Scenario FAILED: No steps executed successfully - check service availability");
        } else {
            System.out.println("🎉 Scenario PASSED: All " + totalSteps + " steps completed successfully");
        }
    }

}
